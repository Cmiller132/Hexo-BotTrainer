Round 8b of the VCF-width task — continuation of round 8 (same binding brief: docs/TSS_VCF_WIDTH_BRIEF.md + .codex-round8/prompt.txt; your own progress notes are .codex-round8/round8-progress.md — read all). HEAD = 3bc5dd44 (your Fix B commitment domains, committed by the orchestrator; verified prefix-14 389,569 / lz 122,132 new-best / 12-12 matrix).

THE 4M VERDICT IS IN (orchestrator ran it; log .codex-round8/0l-full-4m.log): full 0l4291i_live = UNKNOWN at 4,000,000 certified nodes, 441,607 tt_hits, 7,000.6s. Per your own plan, Fix A is now authorized. But FIRST, one cheap diagnostic that may change everything:

TT SATURATION ANOMALY (orchestrator observation, verify it): the 1M run (.codex-round8/0l-full-1m.log) reported 441,328 tt_hits; the 4M run reports 441,607 — only ~300 additional TT hits across 3,000,000 additional nodes. This flatline suggests the hardcoded 512MiB table (tt_bytes_cap: 512 << 20 in tss_corpus.rs) saturates near the 1M mark, after which new positions are not retained, transposition reuse dies, and marginal nodes are nearly worthless. If true, the 1M->4M spend was grinding without memory, and NO cap increase will ever close 0l until the table can hold the working set.

MARCHING ORDERS:
1. VERIFY the saturation hypothesis from the engine's own telemetry (retained-entry counts, table-full/stage-refresh events — you built this instrumentation). Read, do not guess.
2. If saturated: add a TEST-ONLY TT-size override to the backwalk/corpus helpers (e.g. TSS_BACKWALK_TT_BYTES env, default unchanged 512MiB — production semantics untouched), then re-run full 0l at 4M with a 2GiB table (host has ~16GB free; the orchestrator's RAM guard is armed). Projected wall ~2h at your measured ~571 n/s — that fits your shell window; run it synchronously yourself. If the projection drifts past ~2h mid-run mechanics, chunk as before: measure at 2M with 2GiB first (~1h), extrapolate, and hand anything longer to the orchestrator via a clear checkpoint note.
3. If the 2GiB 4M run WINS: announce LOUDLY, then proceed directly to CONSOLIDATION + final gate per the round-8 brief (winning path unconditional; delete losing scaffolds; 5 NO entries; byte-identity; final 19-entry matrix -> .codex-round8/round8-final.md). Note for the final matrix: the gate harness also hardcodes 512MiB — decide and document whether 0l's banked rung legitimately uses the TT override (it is a cap/resource knob, not a semantics change; the brief's ladder governs NODE caps), and make the final matrix reproduce the banked result.
4. If NOT saturated, or the 2GiB run still misses: implement FIX A per the round-8 brief (bounded uncertified DFS racer as scheduling oracle; certified re-solve on racer-win; ProvenFragment import; racer can NEVER mint certificates; memoized once per position; probe budget thousands of racer nodes). Tune on the isolated hard child (prefix 14 + extras 9,-2;12,-1 — certified baseline 225,924), keep the 12-entry matrix + lz + prefix-14 gates green, then ladder full 0l 1M -> 4M, handing >2h runs to the orchestrator.
5. Re-verify closed entries after every engine change. Update .codex-round8/round8-progress.md after every step.

OPERATIONAL RULES: unchanged and binding (no git commits — announce checkpoints; E:-only targets, reuse .target-codex/; rustfmt per-file only; one solve at a time; shared host; this worktree only).
