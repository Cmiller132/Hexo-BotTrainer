Round 6 of the VCF-width task. Round 5 finished a clean engine step but was killed by a host app restart before re-banking lz60mfb or cracking 0l4291i_live. Read docs/TSS_VCF_WIDTH_BRIEF.md in full (ADDENDUM 2 + 3 remain binding).

COMMITTED STATE (orchestrator commits; you cannot):
- HEAD = 199d078d "round-5 checkpoint — tier prior root-only + sequential-root honors tier". This is your build base. It resolves round-4's audit flag: the width-tier prior is now threaded into the sequential-root probe path that 0l actually takes, gated strictly to depth 0 (prefer_width_tier_at_depth). Canonical unordered pairs are unconditional in wide mode. PN_INFINITY saturation fix is present with tests — keep green, do not re-implement.
- 1583aff6 = round-4 consolidation. d7855f6d/a5a4d783 = round-3 toolkit (tau-init, K_b kernel, L13 sparse witnesses 3/5, fork-degree ordering, canonical pairs). lz60mfb was banked WIN 213,854 at the 1M rung under the round-3 tau checkpoint.

ROUND-5 GROUND TRUTH (verified from its logs — binding, do not re-derive):
- 12/14 easy WIN entries stay GREEN at <=100k under HEAD with DEFAULT settings (no env vars): 0hz3hty 3132@10k, acly7kb 75@10k, g2xx6wl 5610@10k, hu01jk4 380@10k, jh7yo7y 1681@10k, jnzzmcm 8032@10k, xsnfyll 77@10k, zrugh2x 51733@100k, strongloss_a_prefix6 15923@100k, strongloss_b_prefix8 682@10k, hayes_turn16 14888@100k, hayes_placement31 14888@100k. matrix-12 failures=0.
- DECISIVE NEGATIVE RESULT on 0l4291i_live: with the root tier prior correctly applied to 0l's sequential root, 0l is UNKNOWN at 100k (tier ON and OFF) and UNKNOWN at the 1M rung (1,000,000 nodes, ~13 min). The root-move-ordering hypothesis (rounds 3-5) is EXHAUSTED for 0l. STOP iterating on root pair selection / tier priors for 0l — that lever is spent.

THE 0l PROBLEM IS INTERIOR SEARCH SHAPE, NOT ROOT ORDERING. Diagnosis anchor: the reference solver proved 0l in 1,058 nodes with 733 leaf solves in 264s (pdspn). Our engine burns >1,000,000 nodes and cannot. That ~1000x deficit is not width (all 181 line placements are inside the count>=2 universe — settled in ADDENDUM 2), not budget (memory verified not the limiter at 1M), and not root choice (round 5). It is INTERIOR MOVE ORDERING and/or LEAF/FRONTIER RESOLUTION. Your job is to diagnose which and fix it. Hypotheses to test (you choose based on evidence — you have ultra reasoning and full repo access):
  (a) Leaf/frontier resolution: "733 leaf solves" strongly suggests the reference resolves deep frontier subpositions with a cheap bounded threat/VCF leaf solve instead of expanding them as full PN subtrees. Check whether our engine expands, as full subtrees, positions the reference kills at a leaf. If so, add/strengthen a bounded win-in-few-plies leaf detector at frontier nodes (soundness: WIN leaf proofs only, never a false WIN).
  (b) Interior ordering fidelity: the round-3 toolkit (tau-informed pn/dn init, fork-degree ordering, K_b kernel) is in the engine — VERIFY it is actually steering selection at depth > 0, not just at the root. A common bug: priors/kernel applied at generation but interior re-selection falls back to uniform PN. Instrument and confirm.
  (c) Deep TT transposition sharing: round 3 predicted canonicalization halves 0l's work by sharing transposed pair-continuations in the TT. Confirm the deep TT is actually deduping 0l's transposed lines (tt_hits should be high and growing); if transpositions are NOT meeting in the table, fix the key/canonicalization so they do.
  (d) Only AFTER ordering/leaf improvements land and 0l still misses 1M: try the 20M rung (the gate's ladder explicitly allows 10k->100k->1M->20M for WIN entries).
- Use the backward-walk protocol (BRIEF ADDENDUM §2) against forcing_corpus_lines.txt to localize the bottleneck TURN: round 3 found prefix 12 is the first failing checkpoint. Apply the reference line prefix, solve at 10k, decrease the prefix turn-by-turn until it stops proving; the first failing turn tells you exactly which mechanism (interior generation, reply set, leaf eval, or ordering) is missing at that depth. Report the localization before doing broad engine surgery.

MARCHING ORDERS (in order):
1. Build HEAD (199d078d) reusing the existing .target-codex/ (incremental — do NOT delete it, do NOT rebuild from scratch). Run the full unit suite (cargo test --release -p hexfield_eq, no filter, no --ignored) + the 12-entry <=100k matrix at DEFAULT settings. Must stay 12/12 WIN and full suite green. Fix/revert if anything regresses. Announce this as a checkpoint boundary.
2. Re-bank lz60mfb on the CURRENT engine: try 100k first (round-3 localization predicted canonicalization roughly halves the first-defender-universal work; the 213,854-node 1M bank predates canonical pairs being unconditional). If 100k misses, confirm and bank the 1M rung. Announce the banked rung + node count.
3. Attack 0l4291i_live per the interior-search-shape analysis above. Backward-walk to localize first, then fix the localized mechanism. Iterate at caps <=100k while diagnosing; only spend big caps (1M, then 20M) once the node-efficiency is actually improving (watch nodes-to-proof on shallower 0l prefixes drop toward the reference's ~1058). Announce checkpoints as you go.
4. Re-verify ALL previously closed entries at their proven caps after EVERY engine change (no silent regressions).
5. Final gate: full unit suite green; all 14 expect=WIN prove on the ladder (0l may use the 20M rung); zero WIN on the 5 expect=NO entries; narrow/default (WidthOptions::default()) byte-identical to today. Finish with ONE clean full-gate matrix over all 19 corpus entries at DEFAULT settings and report per-entry rung + node counts.

OPERATIONAL RULES (binding):
1. You CANNOT git-commit (sandbox blocks worktree git metadata). Announce checkpoint boundaries clearly in your progress messages; the orchestrator commits at those boundaries. Never treat commit failure as a blocker.
2. ALL cargo target dirs stay on E: inside this worktree (.target-codex/ exists — reuse it; use .target-codex-iso/ only if you need an isolated build). NEVER build under C: temp — C: has limited space and the host has crashed from C: exhaustion before. Delete only disposable throwaway targets when done, never .target-codex/ mid-round.
3. NO workspace-wide formatting. `cargo fmt` reformats unrelated files even with a file argument. Use `rustfmt --edition 2021 <file>` on ONLY the files you edit.
4. HOST IS SHARED AND UNDER LOAD: a live GPU training run and a live Lean (Mathlib) Codex campaign are running concurrently on this 32 GB host, which has crashed from RAM exhaustion before. Keep cargo build parallelism modest (CARGO_BUILD_JOBS=4 or lower); do NOT launch parallel heavy cargo jobs; run one solve at a time. Never touch anything outside this worktree.
5. Work only in .claude/worktrees/tss-vcf-width on branch claude/tss-vcf-width.

Write progress notes and any A/B logs under .codex-round6/ for provenance.
