# Strategy stealing in engine Hexo, round 9: the fast outcome and the slow-winner controller

This round gives a negative-but-sharp answer on the fast front and a complete
named first-cycle answer on the slow front.  On every S49 fast
terminal history, the reserve corridor is not merely lost near the terminal:
the post-S15 count offset excludes `RES_1` at every relevant F query.  The
terminal-proximate transition is exactly

`(mu_H,mu_R)=(2,>=3) -> (1,>=2)`,

so the `mu_R=3` slice is S58's `(2,3)` first-event cliff and the `mu_R>3`
slice lacks even the pre-event scalar CAD certificate.  Two ordinary real
appends, the section-53 paired-final-event convention, the S59 reserve, and
S63's S-role stop do not convert that event into a contradiction.  They
therefore prove an obstruction to those proposed conversions, **not** the
nonexistence of a fast alleged winner.  The fast game outcome remains
**OPEN**.

For a slow alleged winner, `d_sigma(h)>=9` still means only that some shadow
counterplay survives through local placement eight; it does not protect
S50's particular branch.  S70 bypasses that quantifier gap.  A new
line-seeded, `tau_E=0` handler gives, for every fixed pure `sigma`, the exact
first-disposition cover requested for slow winners: qualified S58 cliff,
physical `Shat` stop, or a full current `R_1`-admissible entrance (with an
optional immediate sound real-F closure).  This is a first-cycle selected-
history theorem, not indefinite `R_1` recurrence and not a resolution of the
fast outcome nested inside the cliff.  S69 separately gives an exact S-pair
test for preservation of scalar `RES_1`.

## 74. Statement boundary and binding inherited state

### 74.1 Roles, target, and proof-status discipline

The real board has opener `F` and second player `S`.  The shadow board has
opener `Shat` and alleged winning second player `Fhat`, whose fixed pure
strategy is `sigma`.  As in the inherited campaign, the strategy-stealing
target is

`NL_F`: there is no alleged-winning pure strategy for `Fhat`.

This artifact does **not** prove `NL_F`.  It uses the following labels
literally:

- **[PROVEN]**: a hand proof from the folded corpus and the production rules;
- **[SKETCH]**: a stated argument with an identified unproved step;
- **[CONJECTURE]**: a proposed statement without a proof;
- **[OPEN]**: an unresolved obligation; and
- **[CITED]**: an inherited result used at its exact folded scope.

All quantified gadgets below are run on the candidate's own legal,
`sigma`-consistent shadow history.  Every physical append persists.  A
coordinate relabel, reserve designation, portfolio assignment, or debt
reclassification changes no board.  Local branches are alternatives only at
their local decision; globally, legality, cadence, P2, P3, P5, P5R,
collision, certificate, service, persistence, and causality remain
conjunctive obligations.

### 74.2 Production-rule facts used [PROVEN]

The six required Rust sources establish the following rule boundary.

1. Coordinates are axial `i16` pairs and hex distance is the maximum of the
   three cube-coordinate absolute differences.  A radius-eight closed ball
   has 217 cells.
2. The opening is the unique placement at the origin.  Thereafter the legal
   store contains empty cells within distance eight of some occupied cell.
   Occupied, unsupported, wrong-phase second-placement, and post-terminal
   placements are rejected.
3. After the opening, a turn has a sequential `FirstStone` and
   `SecondStone`.  A nonwinning first placement keeps the actor and records
   the first coordinate; a nonwinning second placement passes play to the
   opponent at `FirstStone`.
4. A placement is inserted physically before the affected length-six
   windows are tested.  It touches exactly eighteen windows.  A win is an
   active single-colour length-six window filled by the actor.
5. A winning placement makes the engine terminal immediately.  The engine
   supplies no later placement.  The section-53 coupled terminal closure is
   therefore an analysis convention for the two appends already associated
   with one final paired event; it is not an engine move, a third stone, a
   recolouring, or permission to continue either terminal board.

These facts are only rule extraction.  No program execution is evidence in
this artifact.

### 74.3 Folded errata and authoritative open state

Sections 35, 44, 53, 63, and 73 are binding.  In particular:

- section 68 is a **partial interface map**, not a dichotomy;
- S58 quantifies only over query-first, common-phase,
  one-event-per-microstep, one-for-one selectors;
- S61 includes the complete/maximal-extension step before applying
  `d_sigma`;
- the S59 reserve handler replaces canonical service inside `R_1`, and its
  sound terminal closures use section 53's atomic paired-final-event
  convention;
- S59 proves `CAD+F-LOCK^+` only, never canonical `F-LOCK`;
- S61 defeats the tested one-for-one carrier, not `sigma`; and
- fast outcome, slow winners, the quiet/`R_1` complement, and the
  ejection/outer-carrier classes are independent open regions.

The twelve obstacles in `STRATEGY_STEALING_REVIEW_ROUND8.md` are the
authoritative inherited open state.  They are carried in full in section 79;
item 12 remains the sharpest front.

### 74.4 Round-9 notation and quantifier boundary

At a common-live F-role pre-query state retain the inherited unblocked
window families and minima

`mu_R=min{delta_R^F(W): W in U_R^F}`,

`mu_H=min{delta_H^F(V): V in U_H^F}`.

Liveness puts both minima in `{1,...,6}`.  The S59 scalar entrance condition
is

`RES_1 : (mu_H=1 => mu_R=1) and (mu_H>=2 => mu_R<=2)`.       (74.1)

Fix a reachable S15 synchronization `h`.  Write `q_j` for the genuine
common-live F pre-query state immediately before the `j`-th post-S15
`Fhat` prescription, when it exists, and write that prescription `z_j`.
Thus `q_1,q_2` form the first post-S15 F turn and `q_3,q_4` form the second.

Retain

`FAST_8^{S15}={(sigma,h): sigma alleged-winning and d_sigma(h)<=8}`,

and define the complementary named checkpoint class

`SLOW_9^{S15}={(sigma,h): sigma alleged-winning and d_sigma(h)>=9}`. (74.2)

By S60 [CITED], every alleged-winning reachable S15 checkpoint is in exactly
one of these depth classes.  This is a partition by a **uniform bound over
counterplays**, not a partition of individual branches by terminal age.  A
member of `SLOW_9^{S15}` may still win on some particular branch at local
placement six; it must merely have another compatible branch surviving past
local placement eight.  Section 76 uses this distinction explicitly.

The S15/S50/S61 continuation statements retain S50's inherited `Z^2`
idealization.  They transfer to a finite engine instance only while every
selected coordinate remains representable and legal; no i16-boundary claim
is made.

## 75. The fast outcome: exact reserve exclusion, no conversion

### 75.1 A count-to-deficit census on the terminal approach

For either F role `P`, every P-unblocked six-window contains at most all
physical P stones.  Hence

`mu_P >= 6-|X_P|`.                                          (75.1)

This elementary lower bound is enough to audit every reserve checkpoint.

**Theorem S64 (fast terminal-approach reserve census) [PROVEN].**  Fix a pure
`sigma`, a reachable S15 synchronization `h`, and a genuine one-for-one
post-S15 F continuation with no unmatched real-F placement.  Suppose `z_4`
is `Fhat`'s sixth physical stone and wins.  Then the four pre-query states
have the following exact count/reserve data:

| state | `(|X_Fhat|,|X_F|)` before query | minimum information | `RES_1` |
|---|---:|---|---|
| `q_1` | `(2,1)` | `mu_H=4`, `mu_R>=5` | fails |
| `q_2` | `(3,2)` | `mu_H=3`, `mu_R>=4` | fails |
| `q_3` | `(4,3)` | `mu_H=2`, `mu_R>=3` | fails |
| `q_4` | `(5,4)` | `mu_H=1`, `mu_R>=2` | fails |

Moreover, `z_3` hits a shadow deficit-two window at `q_3` and forces the
terminal-proximate transition

`(2,mu_R>=3) -> (1,mu_R'>=2)`                               (75.2)

before the mandatory query of `z_4`.  If `mu_R=3` at `q_3`, this is exactly
S58's `(2,3)` first-event cliff.  If `mu_R>3`, S55 says that scalar
`F-CAD_2^st` is already absent at `q_3`.

*Proof.*  S15 has F-role counts shadow/real `2/1`; each genuine paired F
event adds one to each.  Equation (75.1) gives the first two rows and the
real lower bounds in the last two rows.

Let `V` be a winning six-window completed by `z_4`.  At that event `Fhat`
has exactly six stones total, so all six lie in `V`.  At each `q_j`, exactly
the `j+1` already-present `Fhat` stones lie in `V`, and the later
prescriptions are its remaining holes.  No `Shat` stone blocks `V`, since
`V` later wins.  Thus `V` has deficits `4,3,2,1` at `q_1,q_2,q_3,q_4`.
The total-count lower bound (75.1) matches each value, proving that these are
the exact shadow minima.

At `q_1,q_2,q_3`, (74.1) would require `mu_R<=2`, contrary to the displayed
lower bounds.  At `q_4` it would require `mu_R=1`, again contrary to the
lower bound.  The real append associated with `z_3` lowers any real-window
deficit by at most one, which also proves (75.2).  S55 and S58 give the two
stated slices. QED.

The theorem locates two different facts which must not be conflated.  The
fast line fails `RES_1` at every `q_j` and is **never admitted** to `R_1` at
either eligible `FirstStone` entrance `q_1` or `q_3`, so there is no honest
in-class "ejection event."  The earliest failed entrance is `q_1`.  The
sharp terminal-proximate readiness failure is the `q_3 -> q_4` event in
(75.2).  Calling only the latter an ejection would falsely imply earlier
reserve membership; `q_2` and `q_4` are readiness checks, not rolling-cycle
entrances.

Even the best real geometry on the `mu_R=3` slice is one event late.  If the
two real appends both hit one deficit-three window, the paired minima along
the terminal turn can evolve

`(2,3) -> (1,2) -> (0,1)`                                  (75.3)

in shadow/real order.  The last state has a shadow win and only a real
deficit-one window.  That is a prepaid threat for a hypothetical later F
move, not a same-event real terminal certificate; the real turn has ended
and the shadow game has stopped.

### 75.2 Why the S59 prepayment cannot be spliced into S50

**Corollary S64.1 (reserve-prepayment incompatibility) [PROVEN].**  No
physical `R_1` reserve state can be carried into the S49 terminal approach
while retaining the same S15 base, append-only history, common phase, and
one-for-one F events.  Equivalently, the intersection of the S49 terminal
approach with `R_1` admission at either eligible entrance `q_1` or `q_3` is
empty; `RES_1` also fails at the intervening `SecondStone` queries.

*Proof.*  S64 directly contradicts `RES_1`, a necessary entrance condition,
at every `q_j`.  An already prepaid real deficit-one window would contain at
least five real-F stones.  Before `z_4` the real board has only four, and
after its associated append it has only five.  Importing the complete S59
witness board would therefore import extra physical F stones not present in
the S15 history.  Deleting, recolouring, or treating them as bookkeeping is
forbidden; appending an unmatched F stone leaves the one-for-one common-phase
carrier. QED.

This corollary does not say that reserve prepayment is impossible in other
histories.  S57-S59 already prove that it is possible and nonempty there.  It
says that the S59 corridor and the S50/S49 count cylinder do not compose at
their advertised interfaces.

### 75.3 Two appends, section-53 closure, and the S63 stop

**Theorem S65 (nonconversion by the tested terminal mechanisms) [PROVEN at
the named mechanism scope].**  At an S49 event on a genuine S15-based
one-for-one history, none of the following turns the shadow sixth-stone win
into a real counterstrategy contradiction:

1. using both ordinary real placements of the second post-S15 F turn;
2. applying section 53's paired-final-event convention;
3. importing the S59 reserve on the same physical history; or
4. invoking S63 solely at the reached F-terminal microstep.

*Proof.*  The two ordinary placements are precisely the appends associated
with `z_3,z_4`.  They change the real-F total from three to five; no ordering
or spatial choice can make five stones fill a six-window.  A third real-F
placement is not part of that turn.  If the real second placement is made
before querying `z_4`, common phase and one-event-per-microstep pairing are
lost, and the total is still only five.  After a nonwinning real second
placement, the real actor is S, so an additional F placement is illegal; on
the shadow board `z_4` ends the game.

Section 53 installs the real append and its already-associated shadow append
inside one coupled final event.  It resolves ordering when both appends have
the required physical terminal facts; it creates no additional append and
cannot make the five-stone real position terminal.  Item 3 is Corollary
S64.1.

S63 is an S-role direct-refutation stop.  Its premise is a common-live
mirror-clean first-unsafe S event with two fresh legal image holes.  The
`q_3 -> q_4 -> z_4` segment is an F turn, and S50's preceding displayed
S pair is first-safe and nonterminal.  S63 remains decisive if its own
premise occurs earlier, but that premise is not forced merely by reaching
the S49 F event. QED.

The scope qualifier is load-bearing.  This theorem does not exclude every
asynchronous, phase-lagged, recoded, or outer physical carrier.  Such a
carrier would owe the full reverse-legality, terminal-transfer, strategy
domain, persistence, and causality ledger; none is constructed here.

### 75.4 The two games immediately after the misalignment

**Lemma S65.1 (post-S49 real-state negative control) [PROVEN].**  On S50's
S49 branch, the shadow engine has ended with an `Fhat` win.  The real engine
is live at S `FirstStone`, with exactly five F stones and four S stones.
Moreover, real S has a legal turn which either wins physically for S or
occupies every cell that would let the existing five F stones win in one
further placement.  This does not decide the later real game.

*Proof.*  The terminal and real phase statements follow from the engine rule
and S49's count: nonwinning real `k_4` completes F's second placement and
passes the real actor to S, while shadow `z_4` is terminal.  S50's real pair
raised S from two stones to four.

With five total F stones, every immediate F-winning window must contain all
five.  The family of its empty holes has size at most two.  If there are two,
the five stones are consecutive on one axis and the holes are the two outside
endpoints; both are empty, supported, legal, and distinct.  Real S can occupy
both in its two placements.  If there is one, S occupies it first and uses a
legal padding cell second; after the first append S has only five stones, and
at most two cells could complete an S six, so a supported empty padding cell
outside that finite set exists.  The zero-hole case uses two such fillers.
If either prescribed S append wins, the real engine stops with S winning;
otherwise all pre-existing immediate F holes have been blocked.  F may create
new geometry on later placements, so no eventual outcome is inferred. QED.

This is the literal post-event answer.  The coupled shadow history cannot be
continued because its engine is terminal; the real game can continue, but
`sigma` supplies no further shadow prescriptions from that terminal node.
Even the current real one-move threats do not force an F win through S's next
turn.  A new outcome argument, not a closure convention, is required.

### 75.5 Fast-outcome verdict

**FAST OUTCOME: OBSTRUCTION PROVEN; GAME OUTCOME OPEN.**  S64-S65 answer the
specific Round-9 attacks.  The reserve corridor cannot enter the fast line;
the exact terminal-proximate reserve failure is located; two real placements
and section-53 closure cannot supply a sixth real F stone; and S63 has no
automatic F-turn premise.  But a legal `Fhat` win on a counterplay is
consistent with the alleged-winning claim.  Real-F nontermination proves
failure of this coupling, not failure of `sigma`.  No theorem "no fast
alleged winner exists" follows, even for `FAST_8^{S15}` or `FAST_<14`.

## 76. The slow-winner controller: an exact ingress boundary and the surviving gap

### 76.1 Reserve admission cannot begin at the S15 entrance

The count obstruction has a slow-history consequence independent of any
terminal assumption.

**Lemma S66 (S15 reserve-ingress timing) [PROVEN].**  On every genuine live
one-for-one continuation from an S15 synchronization:

1. `RES_1` fails at `q_1`, `q_2`, and `q_3`;
2. at `q_3`, `mu_H>=2` and `mu_R>=3`;
3. at `q_4`, `RES_1` can hold only with `mu_H>=2` and `mu_R=2`; and
4. consequently the first possible S59 rolling-cycle **entrance** after S15
   is the common F `FirstStone` checkpoint following completion of the
   second post-S15 F pair and the next nonterminal S/`Shat` pair.

*Proof.*  The count pairs at `q_1,q_2,q_3,q_4` are respectively
`(2,1),(3,2),(4,3),(5,4)` in shadow/real F stones.  Equation (75.1) gives
the lower bounds

`(mu_H,mu_R)>=(4,5),(3,4),(2,3),(1,2)`.

The first three pairs violate the `mu_H>=2 => mu_R<=2` clause of `RES_1`.
At `q_4`, four real-F stones force `mu_R>=2`.  If `mu_H=1`, `RES_1` would
require the impossible `mu_R=1`; if `mu_H>=2`, it requires `mu_R<=2`, hence
exactly `mu_R=2`.  Finally, an S59 rolling cycle begins at common F
`FirstStone`, whereas `q_4` is `SecondStone`.  The next such checkpoint comes
only after `z_4` and its associated real append are both nonterminal and the
following two S-role events complete nonterminally. QED.

Thus “the S59 corridor absorbs the post-S15 opening cycle” cannot mean that
the opening checkpoint was already in `R_1`.  The only possible claim is an
**ingress** theorem: use the second F pair to prepay, protect an adequate real
window through the following S pair, and meet every other `R_1` entrance
clause at the next F checkpoint.  S57 proves precisely one such ingress
mechanism when the `q_3` state is `(2,3)`, `tau_E<=1`, and the actual pair is
dynamically portfolio-quiet.  S50 gives only `tau_E<=2` and does not give
those minimum or quietness hypotheses.

### 76.2 What the slow depth bound actually selects

**Lemma S67 (slow surviving-branch lemma) [PROVEN, shadow scope].**  For every
`(sigma,h) in SLOW_9^{S15}`, there exists a complete/maximal legal `Shat`
counterplay whose genuine `sigma`-consistent shadow history is nonterminal
through local placement eight after `h`.  In particular, on that history the
two post-S15 F turns and the two intervening `Shat` turns shown in S60's
cadence table all occur.  This statement supplies no real-board lift.

*Proof.*  Since `d_sigma(h)>=9`, the integer eight does not bound every
complete/maximal counterplay.  Hence one such counterplay has no `Fhat` win
within its first eight local placements.  It cannot end earlier with a
`Shat` win, since that legal counterplay would contradict the premise that
`sigma` is winning.  A live finite Hexo position on the unbounded board has a
legal placement, so maximality cannot truncate the branch for lack of a
move.  The branch therefore contains all eight placements and remains live.
No assertion about paired real coordinates follows from a fact about the
shadow counterplay tree. QED.

The last sentence is the controller obstruction.  S50 first chooses a legal,
first-safe real pair `y_1,y_2` and thereby fixes one particular liftable
shadow pair and the reached `sigma` prescriptions.  S67 says that **some**
shadow counterplay survives; it does not say that S50's pair survives.  A
slow strategy may win at local placement six on S50's particular history and
still have a different branch witnessing `d_sigma(h)>=9`.  Conversely,
choosing S67's surviving shadow coordinates first and pulling them back to
the real board would reopen S18/S13 reverse legality, current support,
collision, first-safety, certificate, and causality.  No inherited theorem
performs that lift.

This also shows why “slow” and “an S49-age terminal branch” are not disjoint
notions.  The fast/slow classes classify a uniform strategy depth; S49
classifies the physical age of one reached terminal event.

### 76.3 The exact opening-F-pair ingress sieve

Condition now on selecting a coupled branch which is live at `q_3`.  Counts
alone give `mu_H>=2` and `mu_R>=3`, and S55 makes the remaining local cases
exact.

**Lemma S67.1 (opening-F-pair ingress sieve) [PROVEN from S55-S58 at their
folded scopes].**  Before any physical stop, exactly one initial scalar row
holds:

| `q_3` state | CAD/admission fact | Actual-pair disposition |
|---|---|---|
| `mu_H=2`, `mu_R>3` | `F-CAD_2^st` is already absent by S55 | failed ingress before the first query; not an `R_1` ejection |
| `mu_H=2`, `mu_R=3` | one-debt CAD exists, but `RES_1` fails | first-event hit gives qualified S58; if both events are dynamically quiet and `tau_E<=1`, S57 gives a sound stop or prepays to `mu_H=2,mu_R<=2`; first quiet/second aging and `tau_E=2` remain outside those theorems |
| `mu_H>=3`, `mu_R>=3` | CAD exists because its near-window domain is empty, but `RES_1` fails | the pair may create/age new near windows; no inherited theorem forces reserve prepayment, service compatibility, or next-checkpoint admission |

*Proof.*  The count lower bounds are (75.1).  With `mu_H>=2`, S55 says CAD
exists either when `mu_H>=3`, or when `mu_H=2` and `mu_R<=3`; the real count
turns the latter inequality into equality.  In the equality row, S58 is
exactly the first-hit case and S57 is exactly the two-query dynamic-quiet,
`tau_E<=1` case.  Their hypotheses exclude, rather than decide, second-event
aging and `tau_E=2`.  S57's conclusion is physical prepayment at the end of
the F pair; S66 shows it is not yet a rolling-cycle entrance. QED.

The `tau_E=2` row is a sharp independent residue.  S50 proves
two-stone-serviceability, while S57 and `R_1` require at most one service
cell.  S62.2 starts only after an actual nonwinning `tau_E>2` common handoff,
so it does not cover `tau_E=2`.  Likewise, a first quiet/second aging pair is
neither S57 nor S58.  A new handler would have to join first-event prepayment,
second-event catch, and the actual one- or two-cell service without an
unmatched append.

### 76.4 Complete first-cycle interface partition

The requested three outcomes do not exhaust the inherited rules.  The
following theorem records the complete interface without calling it a
controller.

Fix `(sigma,h) in SLOW_9^{S15}` and any causal append-only common-phase
one-for-one **extension attempt** which begins with S50's guaranteed prefix
or another explicitly legal lift and tries to reach the next common F
`FirstStone` checkpoint after local placement eight.  Give terminal events
priority, then the specifically isolated S58 event, then other mandatory
live-handler failures, and finally target-checkpoint entrance classification.

**Theorem S68 (slow first-cycle partition with explicit residue) [PROVEN as
an interface partition].**  Every such extension attempt has exactly one
first disposition under the stated priority:

1. **Physical terminal.**  A real-F terminal is a sound stop; an aligned
   paired F/`Fhat` terminal is a sound co-terminal stop; a physical `Shat`
   terminal on the candidate's own history is the direct counterstrategy
   contradiction; a real-S terminal is sound only with the required P5R or
   distinct common-only physical terminal transfer; and an `Fhat` terminal
   with real F nonterminal is the S49/outcome-open disposition.
2. **S58 cliff.**  Before full entrance or another stop, a common-live F
   `FirstStone` state has `(mu_H,mu_R)=(2,3)` and the actual first
   prescription hits a shadow deficit-two window.  Every query-first,
   common-phase, one-event-per-microstep, one-for-one selector then loses
   `F-CAD_2^st` before the second query.
3. **Strict live ejection before the checkpoint, other than item 2.**  Some
   mandatory legality, phase, first-safety, P2/P3, collision, old-certificate, service,
   `tau_E<=1`, P5/P5R, common-only transfer, persistence, or causality clause
   fails.  S52's wrong-role occupancy, fresh-illegal certificate,
   first-unsafe coordinate, and high-transversal cases are examples; S53 and
   S63 cover only their named subcases.  Voluntary truncation at a live
   prefix is a continuation/coverage failure in this item.
4. **Full `R_1` entrance.**  The attempt reaches a common-live F
   `FirstStone` checkpoint satisfying `RES_1`, `tau_E<=1`, every inherited
   live `A_FS2` clause with reserve service replacing canonical service, and
   all causal/certificate/nonterminal conditions needed to start the next
   reserve-generated cycle.
5. **Live quiet/`R_1` complement.**  The attempt reaches a live state not in
   items 2-4.  This includes `RES_1` with another strict entrance clause
   absent; scalar underfunding other than `(2,3)`; a `(2,3)` state whose first
   prescription is quiet; second-event aging; `tau_E=2`; or an S pair which
   destroys the designated reserve while leaving the engine histories live.

*Proof.*  Engine terminality versus liveness is exhaustive.  On a live
prefix, the specifically named S58 premise either occurs first or not.  In
the latter case, either every other mandatory coupled-handler clause through
the target checkpoint holds or the first failure gives item 3.  At a reached
live checkpoint with those clauses intact, the full `R_1` entrance predicate
either holds, giving item 4, or does not, giving item 5.  The priority makes
the five labels disjoint.  The physical interpretations in item 1 are
S40/S49/S51/S52/S62 and section 53 at their folded scopes. QED.

This is intentionally not a synthesis theorem.  Items 1's misaligned
`Fhat` case, 3, and 5 are genuine residues, not synonyms for sound stop,
reserve admission, or S58.  The slow-depth premise does not remove them.

### 76.5 S50-only controller verdict

**THE DESIRED THREE-WAY CONTROLLER DOES NOT FOLLOW FROM S50 PLUS DEPTH
ALONE.**  S66 proves the earliest
possible reserve-ingress time.  S67 gives a strategy-own surviving shadow
branch.  S68 gives a complete first-cycle interface partition.  What is
missing is one causal physical selector which simultaneously chooses a
surviving shadow pair, supplies its legal real counterpart, and forces every
live branch out of S68 items 3 and 5 while also resolving item 1's S49
outcome.  Neither `d_sigma>=9` nor S50/S59 separately supplies that selector.
Section 78 supplies a stronger custom line-seeded first-cycle handler; this
paragraph records only the failure of the inherited S50/depth composition.

## 77. Quiet and reserve membership under an S pair

### 77.1 An exact scalar preservation test

Consider the S-role portion of one proposed reserve-one rolling cycle.  Just
after its nonterminal F pair, let `p` be the common-live state, let `W_*` be
the designated least real F-unblocked window attaining `mu_R`, and let the
new real-S pair be `Y={y_1,y_2}`.  The shadow pair is written
`Yhat={yhat_1,yhat_2}`.  F stones do not change during this segment.

For `a in {1,2}`, define the pre-pair adequate real reserve family

`A_a(p)={W in U_R^F(p): delta_R^F(W)<=a}`.                 (77.1)

If the pair completes live, let `mu_H'` be the exit shadow minimum and set

`a'=1 if mu_H'=1, and a'=2 if mu_H'>=2`.                   (77.2)

**Theorem S69 (exact S-pair scalar reserve test) [PROVEN].**  Suppose `p`
satisfies `RES_1` and the two S-role pairs complete legally and
nonterminally.  Then:

1. `mu_H' >= mu_H`; shadow-S stones can only block and remove Fhat-unblocked
   windows;
2. the exit satisfies scalar `RES_1` exactly when

   `there exists W in A_{a'}(p) with W intersect Y=empty`;  (77.3)

3. avoidance of `W_*` by both real-S coordinates is sufficient for (77.3);
   and
4. hitting `W_*` is not by itself a scalar failure: scalar readiness survives
   exactly when another window in `A_{a'}(p)` avoids both real-S cells.

*Proof.*  A `Shat` append adds the opposing colour to any Fhat-unblocked
window it meets, removing that window from `U_H^F`; it changes neither Fhat
stones nor deficits of surviving windows.  Thus the shadow minimum cannot
decrease.  The same physical statement on the real board says that the new S
stones remove exactly the old F-unblocked windows meeting `Y`; every
surviving window retains its deficit.  Therefore the exit real minimum is at
most `a'` exactly when (77.3) holds.  By (74.1), that is precisely the real
half required when `mu_H'=1` or `mu_H'>=2`; liveness excludes deficit zero.

For item 3, if the entrance has `mu_H>=2`, `RES_1` gives
`delta_R^F(W_*)=mu_R<=2`, and item 1 gives `a'=2`.  If the entrance has
`mu_H=1`, then `mu_R=1`.  When `mu_H'` stays one, `W_*` is adequate for
`a'=1`; when shadow blocking raises the minimum, the same window is adequate
for the relaxed threshold two.  Avoidance keeps it unblocked in all cases.
Item 4 is the exact existence statement (77.3). QED.

The theorem gives a useful joint classification.  If shadow S leaves a
deficit-one Fhat window alive, real S must leave some real deficit-one window
alive.  If shadow S blocks every such shadow window and raises `mu_H'` to at
least two, real S need retain only a deficit-at-most-two reserve.  Shadow
blocking is therefore scalar-neutral or helpful; real S is the only role
which can destroy the real reserve family.

### 77.2 Scalar preservation versus strict `R_1` membership

**Corollary S69.1 (strict membership classification) [PROVEN at S59's
defined cycle scope].**  For an already admitted reserve-generated F pair,
the following S-pair behaviors are distinct:

- **Strict preservation:** both real-S cells avoid the designated `W_*`, the
  pair is causal, first-safe, certificate-fresh, service-admissible and
  nonterminal, and the common-live exit has `tau_E<=1`.  These are exactly
  S59's S-pair admission clauses, so the segment remains in `R_1` and S69
  supplies scalar preservation.
- **Designated-reserve exit with scalar survival:** a real-S cell hits
  `W_*`, but (77.3) holds using an alternative adequate window.  The prefix
  exits strict `R_1` by definition even though `RES_1` survives.
- **Scalar reserve break:** the real-S pair meets every member of
  `A_{a'}(p)`.  Then `RES_1` fails at the exit.
- **Non-scalar strict exit:** `W_*` is avoided and scalar `RES_1` survives,
  but first safety, certificate freshness/legality/role, service admission,
  nonterminality, common phase, or `tau_E<=1` fails.  S59 cannot be applied.

*Proof.*  The scalar claims are S69.  The strict claims are the five clauses
in the definition of a reserve-one rolling cycle, including the binding
replacement-service and terminal-closure semantics. QED.

Dynamic portfolio quietness belongs to the **next F prescriptions**, not to
the preceding S coordinates.  An S pair can preserve the physical reserve
state and still be followed by an aging first prescription; conversely, a
quiet F pair does not repair a certificate or transversal failure created by
S.  Therefore no S-pair-only theorem can guarantee S57's dynamic-quietness
hypothesis.

### 77.3 The full S-pair admission sieve

Conditional on an admitted predecessor and a reserve-handler-generated
nonterminal F pair, the S-pair portion reaches the next strict `R_1` entrance
exactly when all five gates below hold.  This is a definition-level exact
sieve, not a claim that an outer strategy forces the gates.

| Gate | Preserving behavior | Breaking behavior and disposition |
|---|---|---|
| G1 first real S append | `d_{y_1}>=2` | `d=0`: physical real-S terminal with P5R/common-only duty. `d=1`: first-unsafe live exit; S62.1 excludes a live A/B one-append repair and S63 stops only the mirror-clean subclass. |
| G2 old certificate/filler | required `Shat` append fresh, legal, correct-role, and nonwinning | Fresh/legal/winning is a direct physical stop. Correct-role old occupancy fails strict freshness but S53 may reconcile outside literal strict `R_1`. Wrong-role occupancy and empty-but-illegal/unsupported targets remain open exits. |
| G3 second append/common phase | `d_{y_2}>=1`, associated appends legal/nonterminal, and service admissible | `d=0` is real-S terminal; phase, legality, collision, service, or terminal mismatch exits. |
| G4 designated reserve | `y_1,y_2` both avoid `W_*` | The first hit destroys that designated witness permanently and exits strict `R_1`; S69 decides whether an alternative scalar reserve survives. |
| G5 exit debt/state | common-live exit with `tau_E<=1` | `tau_E=2` exits `R_1` but remains two-stone-serviceable for strict `A_FS2`; `tau_E>2` exits both absent physical reconciliation. S62.2 is only a negative handoff cover. |

At `d_y=0`, `d_y=1`, or a certificate terminal, the engine/stop grammar
takes precedence over any later gate.  Failures can otherwise overlap.  A
sound filler or service real-F win closes and is never continued.

The orthogonal F-pair boundary is also exact at inherited scope:

- at `(2,3)`, `tau_E<=1` plus two dynamically quiet prescriptions gives
  S57 ingress; a first-event hit gives qualified S58; a quiet first event
  followed by second-event aging is not covered by S58;
- at `(2,2)`, S56 catches a first aging event exactly with `k in C_R`, and
  combines it with a nonterminal old-debt completion exactly with
  `k in C_R intersect S_E`; the reserve handler covers admitted
  second-event aging; and
- at `(1,1)`, S59 uses the assigned unique real hole and proves augmented
  `F-LOCK^+`, never canonical lock.

All windows, deficits, legal stores, urgent sets, and certificates are
recomputed after each nonwinning first placement.

### 77.4 Membership-classification verdict

**EXACT FOR SCALAR `RES_1`; CONDITIONAL FOR FULL `R_1`.**  S69 gives a
necessary-and-sufficient hitting test for scalar preservation.  Corollary
S69.1 classifies strict preservation, alternative-reserve survival, scalar
break, and non-scalar strict exit.  By itself it does not lift S67's arbitrary
surviving shadow pair.  S70 instead supplies one separately constructed
candidate-own lift meeting the full current entrance gates.

## 78. A line-seeded first-cycle controller and the partial synthesis

### 78.1 The noncanonical line-seed handler

The S50/depth composition leaves a branch-selection gap, but the requested
first-cycle cover can be obtained without using the slow bound.  The price is
an explicitly new, noncanonical prefix handler on one constructed
candidate-own history.

Fix a pure `sigma`, an S15 synchronization, real opener `F@x`, real S stones
`S@a,S@b`, and the S15 isometry `T`.  The physical stones
`Shat@T(a),Shat@T(b)` certify both S stones, so initialize `E_S=empty`.
Fix all coordinate, window, line, and hole orders once and for all.

**Theorem S70 (line-seeded first-cycle controller) [PROVEN at constructed
next-entrance scope on `Z^2`].**  There is one totalized, prefix-causal,
append-only, common-phase, one-for-one continuation rule whose first
interface disposition is exactly one of:

1. the qualified S58 `(mu_H,mu_R)=(2,3)` first-event cliff;
2. a physical `Shat` terminal on one of two actual certificate appends--a
   direct counterstrategy contradiction when `sigma` is alleged-winning; or
3. a common-live F `FirstStone` checkpoint satisfying the complete current
   `R_1` entrance predicate: `RES_1`, `tau_E=0`, live first-safety and
   certificate facts, and a fresh legal image of the current singleton debt.

The theorem holds for every pure `sigma`; no `d_sigma(h)>=9` premise is
needed.  Disposition 3 is an entrance to a possible next reserve cycle, not a
completed `R_1` cycle or a proof that the next actual F pair is admitted.

*Proof: line seed.*  Exactly eighteen six-windows contain `x`: six shifts on
each of three axes.  A distinct S stone blocks at most `6-d<=5` of them, on
at most one aligned axis.  Thus `a,b` block at most ten.  Choose the first
real F-unblocked window `W_0` through `x`.  Its other five cells are empty,
supported, and legal.  Since `E_S` and its urgent family are empty, define
the new opening handler to use the first two holes of `W_0` as noncanonical
padding for the first post-S15 real F pair, paired sequentially with the
actual `z_1,z_2`.  Shadow Fhat goes from two stones to four and real F from
one to three, so all four appends are nonterminal and cadence remains common.
The real minimum is now exactly three, witnessed by `W_0` and bounded below
by the total real-F count.

*Proof: guarded S50 variant.*  Choose `y_1 in B_8(a)` outside current real
occupancy, `T^{-1}` of current shadow occupancy, `W_0`, and--if `a,b` are
axis-aligned--their axis line.  The respective union bound is

`5+7+6+17=35<217`.                                         (78.1)

Append `S@y_1`.  It is fresh, legal, first-safe by count, avoids `W_0`, and
`a,b,y_1` are not axis-collinear.  With empty debt append a fixed first
fresh legal filler
`f in B_8(T(a)) \ (O_H union {T(y_1)})`.  Current shadow occupancy has
seven cells, so at least `217-7-1=209` candidates exist; the append is only
Shat's fourth stone.

Now **hypothetically evaluate**, without yet appending, the already fixed
future certificate `Shat@T(y_1)`.  It would be fresh, legal through `T(a)`,
and only Shat's fifth stone.  The resulting fixed shadow prefix determines
the actual next pure-strategy pair `z_3,z_4`; `z_3` is Fhat's fifth stone and
cannot win, so `z_4` is defined.  Choose `y_2 in B_8(y_1)` outside the
current real occupancy, `W_0`, the inverse shadow occupancy after the fixed
certificate, and `T^{-1}{z_3,z_4}`.  At most

`6+6+9+2=23<217`                                           (78.2)

cells are excluded.  Only now physically append `S@y_2`, followed by
`Shat@T(y_1)`.  This preserves the genuine engine chronology; the pure
look-ahead is exactly S50/S12's accepted computation and `y_2` changes no
shadow state.  The image `T(y_2)` is fresh through `z_3,z_4` and legal via
`T(y_1)`.

The resulting debt is `E_S={y_2}`, not empty.  An urgent window would have
deficit at most two and hence, with only four total real-S stones, contain
all four.  That would make the subset `a,b,y_1` collinear, contrary to its
choice.  Therefore `U_E=empty` and `tau_E=0`.  Both real S cells avoided
`W_0`, so at `q_3` the exact real minimum remains three.

*Proof: second F pair.*  Query `z_3`, choose the first currently empty hole
`k_3` of `W_0`, and execute that paired event.  Both appends are nonterminal
by count.  If the pre-event `mu_H=2` and `z_3` hits a shadow deficit-two
window, the pre-event state was exactly `(2,3)` and S58 applies at the
reached common `SecondStone` prefix before the mandatory second query.  This
is disposition 1.

Otherwise, if the old shadow minimum was two, the actual prescription missed
every minimum window; if it was at least
three, one append lowers it by at most one.  Hence the reached `q_4` has
`mu_H>=2`, while `W_0` gives `mu_R=2`, so CAD and `RES_1` hold before the
genuine second query.  Query `z_4` and pair it with a distinct remaining
hole `k_4` of `W_0`.

The absence of disposition 1 forces `z_4` to be nonterminal.  Indeed, if
`z_4` completed a window `V`, its six stones would be all six physical Fhat
stones.  Before `z_3`, `V` would contain the four old Fhat stones and have
holes exactly `z_3,z_4`, with no Shat blocker.  Thus `mu_H=2` and `z_3`
would hit that deficit-two window--disposition 1.  This is the precise sense
in which every possible S49 terminal on the total fallback history is
nested inside the earlier cliff; the S49 **game outcome** remains open.

On the continuing branch real F now has exactly five stones, all five in
`W_0`, so it is nonterminal and `mu_R=1`.  Shadow liveness gives
`mu_H>=1`, hence `RES_1`.  F appends did not change `E_S`, so `tau_E=0` and
`T(y_2)` is still fresh/legal.

*Proof: protected ingress S pair.*  Designate the actual fixed-order least
real window `W_*` attaining the post-pair `mu_R=1`; `W_0` is at least one
such witness.  At this point the real and shadow occupancies have sizes nine
and eleven.  Choose `y_3 in B_8(y_2)` outside real occupancy,
`T^{-1}` of shadow occupancy, `W_*`, and every engine-axis line through a
pair of the four old S stones.  There are at most six such lines and a line
meets a radius-eight ball in at most seventeen cells, so

`9+11+6+6*17=128<217`.                                     (78.3)

Append `S@y_3`, then the protected physical certificate
`Shat@T(y_2)`.  The real append is first-safe; the image is fresh and legal.
After `y_3` there are five real-S stones.  A deficit-one window through it
would contain all five and hence the already noncollinear subset
`a,b,y_1`, which is impossible; every F-unblocked window through `y_3`
therefore has deficit at least two.
If the image wins, stop at disposition 2.  Otherwise it certifies `y_2` and
leaves debt `y_3`.  The next image `T(y_3)` is fresh and becomes legal
through the newly appended `T(y_2)`.

The four old S stones are not all collinear, so among them there is at most
one collinear triple.  The line exclusions make no triple involving `y_3`.
Thus among the five S stones before the second placement there is at most one
collinear triple.  If it exists with axial span `s`, the union of all
length-six windows containing it has size `11-s<=9` for `2<=s<=5`, and is
empty for `s>5`.

Choose `y_4 in B_8(y_3)` outside current real occupancy, the current inverse
shadow occupancy, `W_*`, and that at-most-nine-cell danger union.  The bound
is

`10+12+6+9=37<217`.                                        (78.4)

Append `S@y_4`, then `Shat@T(y_3)`.  The real append cannot win: a six would
use all six real-S stones and force the already noncollinear old subset onto
one axis.  If the image wins, it is disposition 2.  Otherwise cadence reaches
common-live F `FirstStone`.  The remaining debt is `E_S={y_4}`.  Any urgent
window would contain `y_4` and at least three preceding S stones; those three
would be the unique possible collinear triple, placing `y_4` in the excluded
danger union.  Hence `U_E=empty` and `tau_E=0`.  The image `T(y_4)` is fresh
by the inverse-occupancy exclusion, distinct from `T(y_3)`, and legal via
`T(y_3)`.

Both new real-S cells avoid the actual `W_*`.  Theorem S69 therefore
preserves `RES_1`; all other current entrance gates were just checked.  This
is disposition 3.  Every choice uses the first eligible object in a fixed
order.  Outside a displayed predicate the underlying pure rule uses the
least legal move while the analysis certificate exits; no terminal state is
continued.  All stones persist, and the only look-ahead is the fixed-pure-
strategy evaluation already justified in S50. QED.

**Corollary S70.1 (immediate sound closure from the entrance) [PROVEN].**
In disposition 3, if the two ingress S cells are additionally chosen off
`W_0`--add at most six cells to each already strict exclusion bound--then
`W_0` remains an F-unblocked deficit-one window.  At the reached F
`FirstStone`, query the actual next `sigma` prescription and pair it, under
section 53's atomic final-event convention, with `W_0`'s unique real hole.
The real append is legal and completes a physical F six.  The coupled trace
therefore has a sound real-F closure, and the outstanding singleton debt has
no later service duty.

This corollary is a selected-history stop, not a universal response to every
real-S pair.  It also does not refute `sigma`: the only direct refutation in
S70 is a physical `Shat` win.  Its value is that the non-cliff branch can be
closed without assuming protection of `T(y_4)` through another nonterminal
F pair.

**Corollary S70.2 (fast candidates hit the line-seeded cliff) [PROVEN].**
For every `(sigma,h) in FAST_8^{S15}`, S60 gives `d_sigma(h)=6`.  Apply S70's
legal line-seeded counterplay.  The alleged-winning clause in the fast-class
definition forbids S70's physical `Shat`-terminal disposition.  If the prefix
were nonterminal after local placement six, extend it legally to a
complete/maximal counterplay, contrary to `d_sigma(h)=6`.  Counts forbid every
earlier Fhat win, so `z_4` is the sixth-stone terminal.  Its terminal window
then proves that `z_3` hit at `(mu_H,mu_R)=(2,3)`.  Thus the qualified S58
cliff is forced one microstep before S49 on this candidate-own history.  This
still decides only the one-for-one carrier: the fast game outcome after a
total fallback continues through the cliff remains **OPEN**.

### 78.2 Partial interface map, not a dichotomy

The following map states only proved coverage.  Its rows are not asserted to
cover all legal histories, and a local stop/failure may satisfy more than one
diagnostic predicate before S68's priority convention is imposed.

| Reached interface | Round-9 disposition | Exact boundary |
|---|---|---|
| S49 sixth-stone approach from S15 | `RES_1` fails at every query; `R_1` fails at both eligible entrances | S64; one-for-one, common-phase, append-only count cylinder |
| `q_3` with future sixth-stone terminal | `(2,>=3)->(1,>=2)` underfunded aging | S58 exactly on the `mu_R=3` slice; CAD already absent when `mu_R>3` |
| S49 terminal event | tested conversions fail; outcome open | S65 does not cover arbitrary lag/recode/outer carriers |
| `(sigma,h) in SLOW_9^{S15}` | a shadow branch survives local placement eight | S67 is shadow-only and does not lift that branch to the real board |
| a chosen slow coupled extension | five-way first-cycle interface partition | S68 exposes rather than resolves strict ejection and live complement |
| admitted post-F S pair | exact scalar `RES_1` survival test | S69; full `R_1` also requires every non-scalar admission clause |
| line-seeded candidate-own first cycle | S58 cliff, `Shat` refutation stop, or `R_1`-admissible entrance | S70; selected history, noncanonical prefix handler, next entrance rather than recurring membership |
| S70 entrance with its seed also protected | immediate sound real-F closure at the next query | S70.1; selected-history stop, not a universal arbitrary-S response |
| mirror-clean first-unsafe S event | physical direct-refutation stop | S63 [CITED], only at its exact two-image premise |
| admitted reserve-one concatenation | `CAD+F-LOCK^+` maintenance | S59 [CITED], canonical `F-LOCK` still unproved |

The map does not imply that every arbitrary real-S continuation reaches a
positive row.  S70 supplies one causal candidate-own first-cycle history for
every fixed `sigma`; it bypasses S67's branch-selection gap but proves neither
completion nor indefinite recurrence of the next reserve cycle.

### 78.3 New theorem ledger

| Result | Status | What it proves | What it does not prove |
|---|---|---|---|
| S64 fast terminal-approach reserve census | **PROVEN** | Exact counts/minima; no `RES_1` at `q_1`-`q_4`; terminal-proximate `(2,>=3)->(1,>=2)` | No outcome contradiction; no theorem about outer non-one-for-one carriers |
| S64.1 reserve-prepayment incompatibility | **PROVEN** | S59's prepaid state cannot be spliced into the same genuine S15/S49 history | No denial of S59's nonempty reserve class elsewhere |
| S65 tested terminal nonconversion | **PROVEN at named scope** | Two real turn placements, section-53 closure, same-history S59 reserve, and terminal-moment S63 do not convert S49 | No universal impossibility theorem for asynchronous/recode/outer architectures |
| S65.1 post-S49 real-state negative control | **PROVEN** | Shadow is terminal; real is live at S `FirstStone`, and S can cover all existing immediate F holes or win | No later real-game outcome and no coupled continuation from the terminal shadow node |
| S66 S15 reserve-ingress timing | **PROVEN** | First possible S59 cycle entrance is after the second F pair and following S pair | No guarantee that the required ingress conditions occur |
| S67 slow surviving-branch lemma | **PROVEN at shadow scope** | Every slow pair has a genuine `sigma` branch live through local eight | No legal real lift, P2/P3, certificate, service, or reserve membership |
| S67.1 opening-F-pair ingress sieve | **PROVEN from S55-S58** | Exact CAD/prepayment/residual rows for an arbitrary live `q_3` attempt | No selector forcing its favorable source hypotheses |
| S68 slow first-cycle partition | **PROVEN as an interface partition** | Exhaustive prioritized disposition of an arbitrary chosen legal coupled attempt | Generic residue: item 1's misaligned case and items 3 and 5 |
| S69/S69.1 S-pair reserve classification | **PROVEN at stated scopes** | Exact scalar hitting test; strict versus alternative-reserve versus scalar/non-scalar exits | No next-pair dynamic quietness and no strategy-specific lift/admission |
| S70 line-seeded first-cycle controller | **PROVEN at constructed next-entrance scope** | For every fixed `sigma`: qualified S58 cliff, physical `Shat` stop, or full current `R_1`-admissible entrance | No arbitrary-S coverage, completed next cycle, indefinite recurrence, or fast outcome inside the cliff |
| S70.1 immediate sound closure | **PROVEN on S70's positive horn** | Protecting `W_0` gives a legal real unique-hole win at the next paired event | No contradiction to `sigma` and no universal real-F strategy |
| S70.2 fast line-seeded cliff | **PROVEN for `FAST_8^{S15}`** | Every named fast candidate reaches exact S58 one microstep before its forced S49 event on S70's history | No fast-class nonemptiness and no outcome after the cliff |

### 78.4 Objective dispositions

1. **Fast outcome:** the proposed reserve/closure/two-append/S63 conversion is
   obstructed at exact count and phase.  The game outcome after S49 remains
   **OPEN**.
2. **Slow-winner controller:** the requested three-way first-cycle cover is
   **PROVEN** by S70, indeed for every fixed pure `sigma`.  The positive horn
   reaches a current `R_1` entrance and S70.1 can close soundly one event
   later.  Indefinite recurrence and arbitrary-real-S coverage remain
   **OPEN**.
3. **Quiet-membership preservation:** scalar `RES_1` preservation under an S
   pair is classified exactly; full `R_1` preservation remains conditional
   on the complete S59 admission list.

The most valuable new theorem is S70.  It removes the arbitrary quiet,
`tau_E=2`, and reserve-loss residues for one constructed first cycle and
proves the requested cliff/stop/entrance cover without misreading the slow
depth bound.  S64 is the sharp fast obstruction which keeps that local
advance separate from the still-open game outcome.

## 79. Binding status versus the review's twelve obstacles

The descriptions below are the complete authoritative Round-8 obstacle list.
The final column records only Round-9 movement.

| # | Authoritative obstacle | Status after round 9 |
|---:|---|---|
| 1 | **Full per-pair and broader zero-lag coverage:** arbitrary-S recurrence, intra-pair changing isometries, total nonisometric point recodings, non-total/window recodings, and indefinite one-repair-per-placement. | **OPEN.** S70 gives one selected finite prefix with the inherited `T`; it proves no arbitrary-S recurrence or recoding carrier. |
| 2 | **Pre-checkpoint and recurring P3 coverage:** one genuine common-phase, serviceable, terminal-faithful history through arbitrary S pairs, not merely concatenations already admitted to `R_1`. | **COMPLETE NAMED FIRST-CYCLE ADVANCE; OPEN globally.** S70 constructs one genuine common-phase, serviceable selected history from S15 to a current `R_1` entrance or earlier stop/cliff. Arbitrary-S and recurring P3 remain open. |
| 3 | **Coverage outside strict `A_FS2`:** missing, blocked, or illegal image cells; unreflected real-S terminals; wrong-role occupancy; unsupported certificates; phase lag; uncertified high-transversal exits; and shadow-F terminals outside admitted alignment. | **ONE NEW AVOIDANCE MODULE; OPEN generally.** S70 avoids these exits by finite exclusions on its own line. S63 still covers only mirror-clean first-unsafe, and the other outer classes remain. |
| 4 | **P5R and common-only real-win transfer through every lag/recode:** including S14's terminal-lag and S25's older-surplus obligations, shielding, certification, F blocking, and same-step physical supply. | **OPEN, unchanged.** S65 explains why section 53 adds no stone. S68 keeps P5R and the distinct common-only transfer as mandatory terminal tests. |
| 5 | **Universal F service and lock:** canonical `F-LOCK`, arbitrary service compatibility, recurring nonterminal portfolio admission, and horizon membership beyond the named `tau_E<=1` reserve class. | **NONCANONICAL FIRST-CYCLE POSITIVE PLUS FAST NEGATIVE; OPEN universally.** S70 installs `tau_E=0` line prepayment and S70.1 closes its positive horn; S64 excludes the S49 line from inherited reserve admission. Canonical lock and recurring arbitrary service remain open. |
| 6 | **Universal shadow-F terminal fidelity and the fast outcome:** later slow/non-reserve terminals still lack same-event real certificates, while S61's fast misalignment only defeats the tested carrier and does not decide `sigma` or `NL_F`. | **FAST CLIFF PRECURSOR FORCED; OUTCOME OPEN.** S70.2 places every named fast candidate at S58 one microstep before S49; S64-S65 obstruct the named conversions. Neither fact refutes a fast alleged winner. |
| 7 | **Reverse legality for spatial carriers:** every inverse/FIFO construction still owes S18, S13, and current unsupported/collision checks. | **OPEN.** S70 constructs forward legal certificate images and needs no inverse. Reverse-lifting S67's arbitrary surviving branch remains an independent problem, not the first-cycle bottleneck. |
| 8 | **Global strategy domain and physical persistence:** every event of a universal construction must remain on one total `sigma`-consistent append-only history with all old stones retaining all rule effects. | **PROVEN for S70's finite history; OPEN globally.** Its actual prescriptions are candidate-own, its fallback is totalized, and every stone persists. Arbitrary-S recurrence is absent. |
| 9 | **Global causality:** outer backing, recoding, and repair must avoid exposing a future real-F coordinate across an S turn. | **PROVEN on S70; OPEN globally.** The prospective `T(y_1)` computation is the accepted S50 pure-strategy look-ahead, while physical appends remain sequential. Window/hole choices are made prefix-causally on the selected history. |
| 10 | **Universal window/certificate maintenance:** arbitrary S-created windows and reassignment, plus simultaneous legality, P2, P3, P5, P5R, and the distinct common-only real-win duty in one recurring physical handler. | **COMPLETE FOR S70'S NAMED FIRST CYCLE; OPEN universally.** S70 jointly checks safety, certificates, service, reserve, phase, and terminal stops there. S69 is the exact scalar subtest; arbitrary recurrence and all distinct simultaneous duties remain. |
| 11 | **High-transversal service and permanent fencing:** S30's exact `tau_E=5` position, S31's six-cell cost, cell availability, interruption, S occupation, reconciliation, and compatibility with P3. S62.2 supplies only a conditional negative handoff cover. | **OPEN, unchanged.** `tau_E>1` remains an S68 live-ejection/complement route; no blocker is installed. |
| 12 | **Strategy-specific reachability, class membership, and outcome:** fast-class nonemptiness, a slow-winner controller, preservation or deliberate exit of quiet/`R_1` membership, handling of real sound stops, and an outcome-level argument after S49. | **REQUESTED FIRST-CYCLE CONTROLLER PROVEN; SHARPEST GLOBAL FRONT OPEN.** S70 gives every fixed `sigma` the selected-history cliff/`Shat`-stop/entrance cover and S70.1 a sound closure. Fast nonemptiness, fast outcome, arbitrary-S coverage, and indefinite slow recurrence remain open. |

No obstacle is discharged universally.  The independence warning from
section 73 remains binding.

## 80. Round-4 agenda and P0-P6/P5R cross-ledgers

### 80.1 Round-4 review's ten-item agenda

| Agenda item | Status after round 9 | Exact advance and remaining duty |
|---:|---|---|
| 1. Pre-checkpoint P3 transfer | **PROVEN on S70's selected first cycle; OPEN generally** | S70 gives one genuine common lift from S15 to entrance/stop/cliff. Arbitrary S pairs and recurrence remain open. |
| 2. P2/P4 at each real-S coordinate | **PARTIAL** | S70 proves freshness/legality for its forward certificate images; S69 classifies a completed pair's reserve effect. **S13's fixed-isometry one-stone FIFO frontier failure remains binding** elsewhere. |
| 3. P5R during every lag/recode | **PARTIAL; OPEN globally** | S68 keeps terminal real S separate from a sound transferred stop. **S14 remains binding** on literal lag, S25 on older surplus, and common-only real wins require their distinct physical transfer. |
| 4. F-service compatibility | **PROVEN on `R_1` and S70's noncanonical first cycle; NEGATIVE on the S49 approach; OPEN globally** | S70 seeds `mu_R=3,tau_E=0`, prepays to one, and closes its positive horn. Canonical `F-LOCK`, arbitrary service, and recurrence remain. |
| 5. Permanent-fence installation | **OPEN** | No new fence is installed. S31's cost, availability, interruption, S occupation, reconciliation, and P3 duties all survive. |
| 6. Reverse P3 legality | **OPEN for spatial transfer** | S70 uses proved forward images and no inverse. S67's arbitrary shadow survivor still cannot be inversely lifted without S18/S13, but that is independent of S70's first-cycle controller. |
| 7. Shadow-F terminal fidelity | **PROVEN on admitted reserve traces; FORCED FAILURE/NO NAMED REPAIR at S49; OPEN universally** | S65 blocks the tested repair mechanisms but does not decide the fast outcome or later slow terminals. |
| 8. Strategy domain and persistence | **PROVEN on displayed finite arguments; OPEN globally** | S70 is candidate-own, append-only, and totalized; every prospective computation is separated from its later physical append. Arbitrary-S recurrence is absent. |
| 9. Causality | **PROVEN locally; OPEN globally** | S70 uses only prefix choices and S50's accepted fixed-pure-strategy look-ahead; actual queries/appends remain sequential. Outer recurrence still owes S12. |
| 10. Strategy-specific reachability and outcome | **FIRST-CYCLE CONTROLLER PROVEN; PARTIAL globally** | S70 closes the requested selected-history first-cycle partition and S70.1 its positive horn. Fast outcome, fast-class nonemptiness, arbitrary-S coverage, recurrence, and `NL_F` remain open. |

### 80.2 P0-P6/P5R cross-ledger

| Obligation | Status after round 9 | Binding disposition |
|---|---|---|
| `P0 STRATEGY-DOMAIN` | **PROVEN on new theorem histories; OPEN globally** | S70 uses actual `sigma` prescriptions on one totalized candidate-own history; its hypothetical successor is computation only. S64/S67 remain genuine at their scopes. |
| `P1 OPENING/CADENCE` | **PROVEN for new modules** | S70's noncanonical padding and line prepayment still append exactly one same-role stone per microstep; every first/second placement and terminal closure follows engine cadence. |
| `P2 REAL->SHADOW` | **PARTIAL** | S70 supplies fresh legal forward certificate images for both selected S pairs; S63 covers mirror-clean terminal images. Arbitrary coordinates, S13, and collision remain open. |
| `P3 SHADOW->REAL` | **PROVEN on S70's selected first cycle and inherited admitted classes; OPEN globally** | Every actual `z_j` in S70 is paired with a reached legal W-hole/padding event. No inverse is used. Arbitrary-S recurrence and the post-cliff fallback remain open. |
| `P4 COLLISION` | **PARTIAL** | S70's explicit inverse-occupancy exclusions prove collision freedom on its finite line; missing, occupied, wrong-role, unsupported, and S13 frontier cases remain elsewhere. |
| `P5 SHADOW-F-TERMINAL` | **PROVEN on admitted locks; CLIFF PRECURSOR at S70; OPEN globally** | Any sixth-stone S49 event on S70's total fallback was preceded by S58. S70.1 supplies a sound/co-terminal real closure only on the positive horn. Fast outcome and other slow terminals remain open. |
| `P5R REAL-S-TERMINAL-REFLECTION` | **PROVEN only in inherited guarded/mirror-clean classes; OPEN globally** | S68 classifies an unreflected real-S terminal as an unresolved terminal-fidelity disposition. S14, S25, shielding, same-step supply, and the distinct common-only transfer remain mandatory. |
| `P6 CAUSALITY` | **PROVEN for S70's first-cycle controller; OPEN globally** | Fixed orders and the accepted pure-strategy look-ahead make every displayed choice prefix-causal; physical `y_2/T(y_1)` ordering remains sequential. Outer recurrence still owes S12. |

## 81. Hostile-review attack surface and complete caveat ledger

### 81.1 Load-bearing limitations

This list carries every Round-8 caveat forward and adds the Round-9-specific
ones.  It is the complete caveat ledger for this artifact.

1. **Scalar feasibility is not a point representation.**  S55 and S69 merge
   no stones, recolour nothing, prove no inverse, and create no coordinate
   map.
2. **State, canonical lock, and augmented lock are distinct.**  CAD or
   `RES_1` certifies readiness.  S59 proves only handler-relative
   `F-LOCK^+`; canonical `F-LOCK` remains open.
3. **A policy must be fixed before play.**  Least choices, prefix predicates,
   and finite special cases must totalize one pure rule.  Returning `None`
   exits a strict analysis class; it is not a move or a portfolio.
4. **Dynamic quietness is load-bearing for S57.**  It is checked at both
   reached F queries.  Pair-start quietness does not prevent the first append
   from creating a near window aged by the second.  S70 bypasses this source
   hypothesis with a different noncanonical line-seed handler.
5. **S58 is first-microstep exact.**  It applies at `(2,3)` only to a
   query-first, common-phase, one-event-per-microstep, one-for-one selector.
   It proves no permanent post-S-pair failure and no theorem about
   asynchronous prepayment.
6. **An unmatched extra real append is not an S40 repair.**  It may be legal
   at a real phase, but it loses the common-phase P3 pairing.  At S49, even
   both available real placements leave only five F stones.
7. **Reserve rolling admission is conditional.**  Every cycle requires
   `RES_1`, `tau_E<=1`, inherited live conditions with replacement service,
   a nonterminal reserve-generated F pair, a causal first-safe
   certificate-fresh service-admissible nonterminal S pair, avoidance of
   `W_*`, and a common-live `tau_E<=1` exit.
8. **A sound real win ends later service duties.**  It is never used as a
   live checkpoint.  A real-S win is not automatically sound: P5R or the
   distinct common-only transfer must be physically supplied.
9. **Positive witnesses are diagnostic unless winningness is proved.**  The
   S57/S59 and S63 cylinders establish physical nonemptiness or stops; their
   displayed strategies are not alleged-winning.
10. **Fast means a uniform counterplay bound.**  `d_sigma(h)<=8` quantifies
    over all complete/maximal counterplays, not one displayed branch.  S24
    gives finiteness but no numerical eight.
11. **The fast class may be empty.**  S61 is conditional.  S64-S65 neither
    prove nonemptiness nor exploit vacuity as an outcome result.
12. **S50 is one guarded construction, not an induction.**  Its safety,
    certificate, and `tau_E<=2` guarantees end at local placement six.  S66
    does not silently iterate it.
13. **S51 is a non-disjoint stop cover, not a controller.**  Membership
    failure, terminal events, and other obligations may coincide.  S68 uses
    an explicit priority only to make its reporting partition disjoint.
14. **S51.1 is never crossed.**  A physical `Shat` win on the alleged
    winner's own history is the contradiction stop; no later state is used.
15. **S62 is a deadline cover, not a constructive shadow win.**  Only S63
    supplies its two terminal image appends on the mirror-clean class.
16. **Mirror-clean alleged-winning reachability is contradictory.**  Its
    diagnostic cylinder may be nonempty for a legal strategy, but an
    alleged-winning candidate reaching its terminal stop is refuted and not
    continued.
17. **P5R and common-only real wins are not synonyms.**  Both physical duties
    remain in every relevant ledger, together with simultaneous legality,
    P2, P3, P5, and P5R.
18. **The high-transversal corollary needs its exact handoff.**  S62.2 does
    not cover phase lag, an earlier terminal, or a present correct-role
    certificate.  S30 reachability and positive reconciliation remain open.
19. **Second placements are sequential.**  Legal stores, deficits, urgent
    sets, certificates, portfolios, and terminal facts are recomputed after
    the first nonwinning appends.  No simultaneous two-stone engine move is
    assumed.
20. **Every physical stone persists.**  Prepayments, fillers, images,
    proxies, certificates, blockers, and reserve stones keep all occupancy,
    support, blocking, collision, and terminal effects.  Rebinding changes no
    board.
21. **Local branches remain globally conjunctive.**  Choosing A, B, C,
    service, reserve, or stop locally never waives P2/P3/P5/P5R, collision,
    persistence, cadence, or causality elsewhere.
22. **No outcome inflation.**  Conditional maintenance plus conditional
    misalignment did not prove a dichotomy in Round 8.  S64's stronger fast
    obstruction plus S69's classification still do not select D2's
    determinacy alternative; `NL_F` is open.
23. **Reserve exclusion is not an in-class ejection.**  S64 proves the S49
    line fails `RES_1` already at `q_1`.  The `q_3 -> q_4` cliff is the
    sharp terminal-proximate failure, not evidence of earlier `R_1`
    membership.
24. **Slow depth gives existence, not branch-uniform survival.**
    `d_sigma(h)>=9` selects at least one counterplay live through eight.  It
    does not protect S50's particular coupled counterplay.
25. **Strategy depth and terminal age are different classifications.**  A
    slow strategy may have an S49-age win on one branch while another branch
    witnesses the slow uniform depth.
26. **Scalar `RES_1` and strict `R_1` are different.**  S69 can preserve the
    former while freshness, first safety, service, `tau_E`, designated
    reserve avoidance, or common-phase conditions eject the latter.
27. **`W_*` avoidance is sufficient scalar protection and mandatory for the
    defined strict cycle, but not necessary for scalar survival.**  An
    alternative adequate window can witness (77.3).
28. **Shadow blocking helps only the scalar F-reserve inequality.**  It can
    still occupy an image, create wrong-role/certificate trouble, win for
    `Shat`, or otherwise break the full handler.
29. **Section 53 is closure semantics, not stone credit.**  It installs both
    already-associated final-event appends before close; it grants no extra
    move and cannot cure S49's physical count five.
30. **S68 is an interface partition, not itself the requested controller.**
    Its complement is honest generic coverage.  S70 proves the controller
    only by constructing a different line-seeded selected history.
31. **The fast obstruction is not a contradiction to `sigma`.**  A physical
    `Fhat` win is consistent with alleged winning.  Failure to transfer that
    win to real F defeats the carrier only.
32. **Reverse legality remains spatially local and current.**  Knowledge of a
    legal surviving shadow pair does not make its inverse supported, empty,
    correct-role, or legal on the real board; S18 and S13 remain binding.
33. **The unbounded idealization is explicit.**  S50-based coordinate-choice
    arguments are on `Z^2`.  A finite/i16 engine transfer is conditional on
    every chosen cell remaining representable and legal; no wraparound or
    boundary behavior is used.
34. **Section 44's transient two-debt state remains physical.**  Rolling
    service may temporarily carry the old and new debts; a filler or
    reconciliation `Shat` win is a physical stop, and a P3-shaped diagnostic
    cylinder is not evidence of compatibility with every alleged winner.
35. **Correct-role reconciliation is recognition, never relabelling.**  S53
    may discharge an old debt only because the required physical `Shat`
    stone already exists.  Wrong-role `Fhat` occupancy cannot be converted
    into a certificate.
36. **Obligations do not collapse under simultaneous wording.**  Satisfying
    P2/P3/P5R does **not** discharge simultaneous legality, P5, or the
    distinct common-only real-win transfer duty.
37. **S70 is a new noncanonical prefix module.**  Its `W_0` padding and
    prepayment are not Definition 46.1's canonical service and do not begin
    under S59, because `RES_1` initially fails.  Local legality/cadence is
    proved directly.
38. **Prospective evaluation is not a physical append.**  After `y_1/f`, the
    rule computes `sigma` on the fixed prospective `T(y_1)` successor, then
    chooses `y_2`, and only then physically appends `y_2/T(y_1)` at the
    common second microstep.  Actual `z_3,z_4` queries remain sequential.
39. **`tau_E=0` is not zero debt.**  S70 carries `E_S={y_2}` and later
    `E_S={y_4}`.  The urgent family is empty; the physical certificates and
    fresh/legal next image remain mandatory.
40. **An entrance is not a completed cycle.**  S70 reaches the full current
    `R_1` entrance predicate.  It does not prove the next reserve-generated F
    pair, protect `T(y_4)` through that pair, or complete/iterate a rolling
    concatenation.  S70.1 instead stops immediately.
41. **S49 is nested in S70's cliff, not resolved by it.**  Every possible
    sixth-stone terminal on the total fallback has the S58 precursor.  An
    interface stop at that precursor leaves the later fast game outcome
    open.
42. **S70 is selected-history, not arbitrary-S coverage.**  Its real S pairs
    are chosen off the seed/reserve and finite danger sets.  S70.1's reserve
    survives because of that choice; an arbitrary real S pair could occupy
    it and reopen the S12/controller duty.

### 81.2 Binding regression matrix

| Regression | Round-9 treatment | Remaining boundary |
|---|---|---|
| S12 preannounced real-F coordinate | S70 selects all actual cells by prefix rules and keeps prospective computation nonphysical. S70.1 is valid because its selected real-S pair was explicitly chosen off the reserve; it is not an arbitrary-S response. | Any recurring, arbitrary-S, outer-backing, prepayment, or recoding selector must still avoid exposing an occupiable future F service. |
| S13 fixed-isometry FIFO frontier | No new fixed-map FIFO claim is made. S70 uses proved forward images; S67/S68 retain the independent inverse-lift problem. | Every fixed-isometry one-stone FIFO or claimed inverse realization still owes the frontier, support, collision, and current legal-store checks. |
| S14 literal one-cell terminal lag | S70's selected S pairs are first-safe/nonterminal and use same-event physical certificates; S68 keeps other real-S terminals unresolved. | Every unguarded literal lag remains excluded; P5R and common-only supply are separate. |
| S18 proxy-supported reverse illegality | S70 proves its forward image legality from successive physical support and infers no inverse. S67's generic survivor stays shadow-only. | Every spatial inverse carrier still owes reverse support and legality at the current prefix. |
| S20 proxy-fabricated `Fhat` win | S70 nests every possible sixth-stone terminal inside S58 but does not decide it; S64-S65 retain the physical five-versus-six obstruction. | Universal canonical lock, fast outcome, slow/non-reserve terminal fidelity, and outer transfer remain open. |
| S25 older-surplus real-S win | S70's selected S placements are nonterminal; S68 keeps all other real-S terminal memory, P5R, shielding, blocking, and common-only transfer explicit. | Every lag/recode branch outside inherited guarded classes still owes a same-step physical outcome. |
| S30 exact `tau_E=5` fork | S68 places `tau_E>1` outside `R_1`; S69's scalar reserve test does not reduce debt. S62.2 remains only a conditional stop cover. | Reachability, phase lag, positive reconciliation, and service beyond two remain open. |
| S31 six-blocker permanent fence | No fence installation is claimed. | Six-cell availability, interruption, S occupation, reconciliation, and P3 compatibility all remain binding. |

### 81.3 Terminal and stop grammar

For detached reading, the valid stop semantics are:

- real F wins before `sigma`: sound real stop; do not continue it;
- paired real-F/`Fhat` wins in one section-53 event: sound co-terminal stop;
- `Shat` wins on a legal candidate-own counterplay: direct contradiction to
  alleged winning; do not continue it;
- real S wins: require physical P5R for a debt-meeting win or the distinct
  common-only transfer, with shielding/certification as applicable;
- `Fhat` wins while real F is live: terminal-fidelity/carrier failure, not a
  contradiction to `sigma`; and
- a live class exit is not a win and not permission to stop the underlying
  total pure strategy.  It remains an outer coverage obligation.

## 82. Sharp next question and attack agenda

The exact next question is:

> At the line-seeded `(2,3)` cliff forced one microstep before every named
> fast S49 event, can a genuinely asynchronous or outer-recoded append-only
> carrier cross the cliff and turn the ensuing game state into a physical
> counterstrategy contradiction; or can one prove that every such carrier
> ejects and decide the actual real-game outcome after S49?

The next fast theorem must decide the game after carrier failure, not propose
another way to count five real stones as six.  S65.1 shows that even the
existing immediate real threats can be covered by the next real-S pair.

The secondary slow question is recurrence: if S70.1's immediate sound closure
is deliberately not taken, can the line/triple-exclusion method be repeated
through the next actual `sigma` pair while preserving the singleton
certificate and every `R_1` gate for arbitrary reached S geometry?  That is
strictly beyond the now-proved first-cycle cover.

## 83. Provenance and work boundary

### 83.1 Repository identity

- Workspace: `E:\Hexo-BotTrainer-hexgt\.claude\worktrees\hunt-gap-raw`
- Branch supplied by the task: `hunt/gap-raw`
- Input/HEAD read during this round:
  `ab0fd965f2c8be07d373c0426e7a457c21f4700a`
  (`ab0fd965`)
- Input subject: `docs(stealing): round-8 hostile review — core S55-S63
  CONFIRMED at scopes, §68 synthesis REFUTED and rewritten as partial
  interface map (§73)`
- The input already contains the post-review fold.  Its section 73 records
  the reviewed/landed Round-8 artifact identity `e93d9d74`; this Round-9
  artifact does not rewrite that historical identity.
- After the proof and source audit, the shared branch advanced externally to
  `9e57ea060462788841d1f8f761db894981b482e9`.  That later commit was not an
  evidentiary input to this round; every claim here remains based on the
  task-pinned `ab0fd965` corpus and the six listed rule sources.
- `STRATEGY_STEALING_ROUND9.md` is a working-tree deliverable.  No commit was
  created, so there is no Round-9 landed hash.

### 83.2 Required corpus read first, in order and in full

The evidence pass read the following before the rule sources:

1. `STRATEGY_STEALING_HEXO.md`;
2. `STRATEGY_STEALING_ROUND2.md` and
   `STRATEGY_STEALING_REVIEW_ROUND2.md`;
3. `STRATEGY_STEALING_ROUND3.md` and
   `STRATEGY_STEALING_REVIEW_ROUND3.md`;
4. `STRATEGY_STEALING_ROUND4.md` including section 35, and
   `STRATEGY_STEALING_REVIEW_ROUND4.md`;
5. `STRATEGY_STEALING_ROUND5.md` including section 44, and
   `STRATEGY_STEALING_REVIEW_ROUND5.md`;
6. `STRATEGY_STEALING_ROUND6.md` including section 53, and
   `STRATEGY_STEALING_REVIEW_ROUND6.md`;
7. `STRATEGY_STEALING_ROUND7.md` including section 63, and
   `STRATEGY_STEALING_REVIEW_ROUND7.md`;
8. `STRATEGY_STEALING_ROUND8.md` including binding section 73, and
   `STRATEGY_STEALING_REVIEW_ROUND8.md` in full.

The Round-8 review's twelve-item list, not the pre-errata section-69 wording,
was used as the authoritative open state.

### 83.3 Rule sources read in full

In the required order:

1. `packages/hexo_engine/rust/src/coord.rs`;
2. `packages/hexo_engine/rust/src/legal.rs`;
3. `packages/hexo_engine/rust/src/rules.rs`;
4. `packages/hexo_engine/rust/src/board.rs`;
5. `packages/hexo_engine/rust/src/state.rs`; and
6. `packages/hexo_engine/rust/src/tactics.rs`.

No `GAP_RAW_*` file was opened or used as evidence.

### 83.4 Hand-proof and mutation boundary

- No Cargo command, Lean command, proof harness, test harness, or game search
  was run.
- Only hand proofs and read-only source/corpus inspection support the claims.
- No engine source or prior campaign artifact was modified.
- Pre-existing unrelated working-tree files were left untouched.
- No git commit, reset, checkout, merge, rebase, or other history mutation
  was performed.

### 83.5 Final status

`NL_F` remains **OPEN**.  The fast outcome remains **OPEN** after a proved
reserve/closure obstruction and a forced line-seeded cliff precursor.  The
slow three-way **first-cycle selected-history controller is PROVEN** by S70,
with an optional immediate sound closure by S70.1; arbitrary-S coverage and
indefinite recurrence remain **OPEN**.  Scalar S-pair reserve membership is
classified exactly at S69's scope.
