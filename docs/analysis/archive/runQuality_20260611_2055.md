# runQuality — dense_cnn_restnet_main_3 @ 2026-06-11 20:55 (epoch 8 complete, ep9 in flight)

**Verdict: HEALTHY-WITH-WATCH-ITEMS. No intervention now. Decision gate = ep10 sealbot eval.**

## Snapshot (latest completed epoch: 8)

| signal | ep5 | ep6 | ep7 | ep8 | read |
|---|---|---|---|---|---|
| decisions/game | 108.4 | 108.8 | 107.5 | **169.7** | one-epoch +57% jump (see below) |
| recorded rows | 10,229 | 10,297 | 10,280 | 16,210 | reuse now 3.0x (in band) |
| train policy CE | 2.686 | 2.835 | 2.663 | 2.881 | mostly target-entropy mechanical |
| train value CE | 0.699 | 0.695 | **0.684** | 0.741 | ep8 rise = composition (deeper rows) |
| P0 winner share | .503 | .589 | .503 | **.567** | recurring skew, not 2-consecutive yet |
| pos/s | 16.8 | 16.3 | 14.8 | 16.2 | fine (ep7 dip was probe contention) |
| truncations | 1 | 0 | 0 | 3 | negligible |

Full-population probe on ep8 (16,121 rows, all 384 games):
- Sharpening overall **+0.204** (healthy; main_2 collapse was +0.100-and-falling). Deep plies +0.244 — strongest yet.
- **WATCH #1 — opening prior softening**: ply[5,16) prior top1 = **0.195** (was ~0.27 ep4–6), AT the 0.20 alarm line; early-ply sharpening weak (+0.043/+0.055). Mechanism: length jump shifted row mix to 74% deep positions → opening rows fell ~12%→8% of gradient share, and the value head can't rank openings yet (far-horizon sign-acc 0.53). This is data starvation, NOT main_2's temperature feedback (root temp flat 1.05; deep game sharpens hard).
- **Value optimism RESOLVED at population level**: bias +0.036 (alarm +0.15). The earlier +0.089/+0.111 were sample/skew effects.
- Value head beats constant hedge on ep8 rows (CE 0.677 vs 0.693); autopsy convpos 0.99, v_std 0.230.

## Why games lengthened (best explanation)

ep8's games are the first played by a net whose value CE crossed the hedge bound (ep7) — better defense legitimately lengthens Hexo games. Deep targets are sharp (contested-game shape), not mush (main_2's wandering shape). BUT note the feedback loop: longer games → EMA up → play-temp halflife up (0.12×EMA) → hotter longer → longer games. Doesn't self-limit by construction.

## Intervention triggers (check each iteration)

1. ply[5,16) full-pop prior top1 < **0.15** at ep9/10 → opening starvation is compounding. Fix: ply-weighted training sampling (upweight opening rows to ~ep5 gradient share). NOT exploration knobs.
2. **ep10 sealbot eval** (next eval epoch; ep5 baseline was 106/128 = 82.8%): regression > ~10 games → treat opening dip as strength-costing; act per #1.
3. decisions/game > **200** → cap play-temp halflife in absolute plies (e.g. min(0.12·EMA, 16)) to break the length feedback.
4. P0 share > 0.55 in 2 consecutive epochs → investigate first-stone parity interaction.
5. Value bias ≥ **+0.15** full-pop on a balanced epoch → A/B policy_surprise off, then batch-calibration penalty (see session notes / MAIN3_RUN_AUDIT.md).
6. Supervisor relaunches > 0 or breaker events → inspect train log immediately.

## How to reproduce this evaluation (for the next hourly iteration)

- Trends: diagnostics/epoch_NNNNNN.json → metadata.result.{selfplay,training} (key is "training", NOT "train").
- Winner balance: hxr scan (see scripts/_main3_health.py).
- Full-pop probe (only when a NEW epoch completed since last runQuality file; CPU-only, ~6 min):
  `wsl: cd /mnt/e/Hexo-BotTrainer-hexgt/scripts && CUDA_VISIBLE_DEVICES= /root/.venvs/hexgt-build/bin/python _m2_probe.py dense_cnn_restnet_main_3 <EP> <EP> 384 _wf_m3_probe_ep<EP>_full.json 0.0`
- Value autopsy: `_value_autopsy.py` (CPU-forced). NEVER run anything on the GPU — the run owns it.
- Append-style: write a NEW runQuality_<YYYYMMDD_HHMM>.md per iteration; compare against this file's numbers.

Prior context: docs/analysis/MAIN3_RUN_AUDIT.md (40 verified findings, ep1–7).
