# Campaign fold confirmation

Date: 2026-07-14.

This was a fresh-eyes confirmation of `docs/_SPEC_FOLD_CAMPAIGN.md` against
the two hostile reviews, the four campaign reports, the repaired checker
scripts, `docs/proof_parts/ES_GLOBAL_BOUNDARY.md`, and the revised normative
document. No mechanical or substantive defect was found. No source file was
changed during this pass.

## 1. The eleven fold blocks

Verdict: PASS.

1. **APPLIED-CORRECTLY.** Normative §6a contains the reachable disjoint-hit
   race, the compressed §§1.2--1.4 obstruction summary, D19, D20, D20a, D21,
   L15, L16, L17, T11, T11.1, both sharpness results, and the verifier
   procedure. The FHW review's R1--R9 repairs are present: the restricted
   scope sentence and node taxonomy, full-cost `F` terminology, completed
   substitution corridor, exact-label requirement, full-clock clause,
   D13/T7 augmentation, defined L16(2) statistic, repaired L17 text, completed
   T11.1 premise and proof, and terminology corrections.
2. **APPLIED-CORRECTLY.** D9 uses `|T| <= 5 for b = 2`; L13 is the reviewed
   `3/5` theorem and proof, including the explicit singleton inference; and
   §9 states at most three witness windows at `b = 1` and five at `b = 2`.
3. **APPLIED-CORRECTLY.** The sentences after L9′ and L12 match the review's
   downgraded text word-for-word after line-wrap normalization. Section 12
   item 2 says `fixed-window virgin radius` and expressly leaves full-union
   sharpness open.
4. **APPLIED-CORRECTLY.** T8.2 gives the index-12 period lattice, all six
   pairs, the `Z_2 x Z_6` endpoint bijection, one phase on each physical line,
   the repaired rigidity argument, and index-minimality. It cites the report
   and checker. The closing consequence states that `k >= 2g+1` is tight,
   with a hex pairing at `k = 7` and none at `k = 6`.
5. **APPLIED-CORRECTLY.** The repaired ES report is installed in
   `docs/proof_parts/ES_GLOBAL_BOUNDARY.md`. Section 10 records all-ties
   greedy refutation, the engine-reachable enlargement, universal clean
   escape, static pairing and damping no-go results, the five-placement and
   three-pair horizons, the finite König reduction, and all five named gaps.
6. **APPLIED-CORRECTLY.** Section 12 item 1 is the prescribed partial
   exact-copy result; item 3 resolves threshold pairing; item 4 retains
   formalization; and items 5--7 record the uniform-wrapper, full-union virgin,
   and ES `GAP-RAW` open problems.
7. **APPLIED-CORRECTLY.** Section 12a contains the frontier table with the
   reviewed R3, R5b, R6, R13, and R15 scopes and the absolute-versus-relative
   pin distinction.
8. **APPLIED-CORRECTLY.** The Round 7 log names the four reports and both
   reviews, records the installable-with-repairs rulings, and identifies R15's
   absolute-pin classification as the campaign's sole refuted classification.
9. **APPLIED-CORRECTLY.** Section 9 contains the exact augmented-certificate
   replacement bullet and the revised `3/5` witness cap.
10. **APPLIED-CORRECTLY.** `_OPEN_FHW_REPORT.md` contains R1--R9;
    `_TIGHTNESS_FRONTIER_REPORT.md` contains every prescribed heading,
    paragraph, frontier-row, and final-map repair; `_OPEN_PAIRING7_REPORT.md`
    contains the repaired rigidity sentences; and `_OPEN_ES_GLOBAL_REPORT.md`
    contains both repaired scope passages. Superseded wording is absent.
11. **APPLIED-CORRECTLY.** The tightness checker has the strengthened exact
    family test, 20-stone assertion, and debug guard. The pairing checker has
    the 419-state assertion and debug guard. The ES checker has the incident
    count assertions, translated state-set bijection, and debug guard. All
    four normal runs succeed.

## 2. Scope downgrades

Verdict: PASS.

The installed claims consistently use the reviewed scopes:

- R5b is absolute only for combined predicate enforcement.
- R6 is absolute only for the combined numeric and universal-survivor
  contract.
- R15 is relative/syntactic, not absolute.
- R3 and R13 state local or fixed-window arithmetic attainment while leaving
  the corresponding full pins open.
- L9′ claims exact-role-rank sharpness and disclaims sharpness of the uniform
  `B` wrapper.
- L12 claims fixed-window attainment and disclaims a pin of the full
  `Z_virgin` union.
- Section 12 item 2 uses fixed-window wording and points to the two remaining
  open frontiers.

No broader superseded claim remains live in the normative document or source
reports.

## 3. Tags, cross-references, and internal consistency

Verdict: PASS.

Every live definition tag is defined once: D1--D21, including D20a;
L1--L7, L9′--L17; T1--T11, including T8.1, T8.2, and T11.1; F1; MI; the
base zone terms; the augmented `^FH` zone terms; and the live verifier labels
(Z2), (Z4), and (Z5′). L8 did not exist in the pre-fold document and is not
referenced. L9 had already been replaced by L9′ before this campaign. The old
Z1/Z3/Z5 names occur only in the preserved historical review log and are not
live verifier references.

No pre-existing tag was deleted or renumbered. Every §6a cross-reference among
D9--D18 and T3, T6, T9, and T10 resolves to the stated grammar, clocks,
coupling, kernel, envelope, and DAG results. Section 6a does not invoke base
T4 for augmented nodes; it correctly invokes T11 through the D13/T7 augmented
clause. That clause is used only at D21 ordinary nodes; D19 gates use exactly
`K(Q)`.

Sections 12, 12a, and 13 agree with §§6a, 8, and 10. They consistently state
the exact-copy debit as partial, `k = 7` pairing as resolved while `k = 6`
remains impossible, exact-rank and fixed-window arithmetic as attained while
the uniform and full-union frontiers remain open, and greedy ES as refuted
while the raw existential claim remains open.

## 4. Collateral damage

Verdict: PASS.

Sections 0--4 and 11 are line-for-line identical to
`docs/_PROOF_PRE_R5_BACKUP.md`. That backup predates Round 5, so the later
baseline for §§5--7 was checked structurally against
`docs/_T3PLUS_ROUND6_CONFIRMATION.md`: every Round 6 definition, theorem,
proof anchor, and integration paragraph remains present. The campaign changes
there are confined to the named D9 cap, L13 replacement, reviewed sharpness
sentences, D13/T7 integration, and inserted §6a. Section 9 retains its prior
measurement and mechanized-validation prose around the prescribed first-bullet
and witness-cap edits. No unexplained change or truncation was found.

## 5. ES boundary copy

Verdict: PASS.

`docs/proof_parts/ES_GLOBAL_BOUNDARY.md` is byte-identical to the repaired
`docs/_OPEN_ES_GLOBAL_REPORT.md`. Both files are 25,084 bytes and have SHA-256
`58126F19F49B55888B116C3E2E1A3149D202FCE089F195B6BC142E1B7D6142C8`.

## 6. Checker runs

Verdict: PASS.

- `python scripts/_tightness_check.py`: exit 0; `all tightness checks passed`.
- `python scripts/_pairing7_search.py`: exit 0; 120 solutions, 419 recursive
  states, and 120 direct-enumeration solutions.
- `python scripts/_es_global_check.py`: exit 0;
  `GREEDY-REFUTATION VERIFIED`, with the 39-placement reachable history.
- `python scripts/_fhw_review_check.py`: exit 0; every displayed check passed.

The three assertion-based campaign checkers refuse optimized execution:

- `python -O scripts/_tightness_check.py`: exit 1 with the required
  `RuntimeError`.
- `python -O scripts/_pairing7_search.py`: exit 1 with the required
  `RuntimeError`.
- `python -O scripts/_es_global_check.py`: exit 1 with the required
  `RuntimeError`.

## 7. Encoding and line endings

Verdict: PASS.

`docs/PROOF_TSS_DEFENDER_ZONES.md` is valid UTF-8 without a BOM. Its 105,852
bytes contain 2,006 CRLF sequences, zero bare LF characters, and zero lone CR
characters. The file ends with CRLF.

Defects: none.

Direct fixes: none.

PASS
