Round 7 of the VCF-width task. Round 6 was stopped by the orchestrator mid-experiment — not for being wrong, but because its findings changed the correct strategy. Read docs/TSS_VCF_WIDTH_BRIEF.md in full (ADDENDUM 2 + 3 remain binding except where this brief explicitly supersedes them).

COMMITTED STATE (orchestrator commits; you cannot):
- HEAD = e29e18b8 "round-6 mid checkpoint". Your build base. Contains round 6's verified results PLUS its cfg(test)/env-gated experimental scaffolding (macro-DFS, DAG choice-PN, sequential-small-choice, frontier fragment probes with certificate import). All experiments default OFF; defaults should equal the verified 199d078d engine.
- Round-6 progress notes: .codex-round6/round6-progress.md (read it).

ROUND-6 GROUND TRUTH (verified from its logs — binding, do NOT re-derive):
1. Full suite green (100/0) and 12/12 easy-entry matrix green at defaults on 199d078d.
2. lz60mfb RE-BANKED on the current engine: WIN 125,020 nodes @ 1M rung (improved from 213,854). 100k rung confirmed insufficient (UNKNOWN at 100k).
3. 0l4291i_live fully localized: the prefix-14 defender universal (defender-to-move, two disjoint live count-4 windows, tau=b=2, four-cell K2 kernel -> 4 canonical defender pairs). THREE of the four pairs prove at 10k. The sole blocker is the off-reference defense (9,-2)+(12,-1), pn=997/dn=663 at 10k. Everything from prefix 15 inward is cheap (LOSS 525 nodes; WIN 72 nodes at prefix 16).
4. Interior first-choice ordering at the hard child is EXHAUSTED as a lever: each of its three first attack choices was forced; none closes within 10k.
5. THE HARD CHILD IS GENUINELY DEEP: bounded-horizon sweeps EXHAUST without proof at depth 20 (7,107 nodes) and depth 22 (12,410 nodes). The reference PV fits in 20 placements, but refuting the off-reference defenses provably requires horizons deeper than 22. No ordering or scheduling trick will make this subtree shallow.
6. Round-6 probe experiments kept returning UNKNOWN for an ECONOMIC reason: leaf probes were charged against the same 10k outer cap (44 probes consumed 9,922/10,000 expansions). Also found+fixed: 128-node certificate-promotion limit could silently discard tactical proofs (hard continuation needs 305 nodes); fixed-horizon probes work (staged-ladder probes wasted quota rediscovering the horizon); tier persistence below root re-rejected; graph-PN profile irrelevant to this node (it is a DN-selected Universal).

STRATEGIC REFRAME (binding): The reference pdspn's "1,058 nodes" was never total work — it was 1,058 INTERIOR nodes plus 733 bounded leaf solves at ~0.36s each = 264 SECONDS of compute. Rounds 3-6 tried to close 0l inside small interior caps via ordering cleverness; round 6 proved the hard subtree is deep and ordering is spent. DIAGNOSIS IS COMPLETE. The ≤100k iteration rule (ADDENDUM §3) applied to diagnosis; it does NOT forbid spending real budget on the close. The gate ladder has ALWAYS allowed a 20M rung for WIN entries — no round has ever run it (rounds 3-5 all stopped at 1M). It is time to measure precisely and then spend deliberately.

MARCHING ORDERS (in order):
1. Sanity spot-check (cheap, ~2 min): build HEAD reusing .target-codex/ (incremental). Run jh7yo7y and xsnfyll at 10k with DEFAULT settings (no A/B env vars). Expect exactly WIN 1,681 and WIN 77. This confirms the committed scaffolding is truly inert at defaults. If it is not, fix that FIRST (defaults must equal the 199d078d engine) and announce a checkpoint.
2. MEASURE THE HARD CHILD ALONE (the one number nobody has): backwalk harness, prefix 14 + extra placements (9,-2);(12,-1), DEFAULT engine, cap ladder 100k then 1M (TSS_BACKWALK_CAP). Report status + nodes at each rung. (~15 min for the 1M run.) This is an attacker-to-move position; a WIN proof here plus the three cheap pairs closes the prefix-14 universal.
   - If it proves at ≤1M: proceed to step 3.
   - If it is UNKNOWN at 1M: report the pn/dn trajectory and STOP engine work; write up the measurement and hand back to the orchestrator (do not start new scaffolding families).
3. SPEND THE 20M RUNG on full 0l4291i_live at DEFAULT settings. Nobody has ever run it. Expected wall time: multi-hour (1M ≈ 13 min; scale roughly linearly, so plan for ~4-5h). Practical mechanics, binding:
   - Launch the solve as a detached logged process (e.g. Start-Process powershell with output redirected to .codex-round7\0l-20m.log) and POLL the log — do not hold one synchronous exec open for hours (tool timeouts killed nothing yet, but do not find out the hard way).
   - MEMORY WATCH: poll the test process working set and host free RAM every few minutes. The PN arena at 20M nodes may reach several GB. If the process working set exceeds ~6 GB or host free RAM drops below 2.5 GB, KILL the run, record the node count reached, and fall back to step 4. Never let the host OOM — it has crashed three rounds already.
   - If 0l proves anywhere ≤20M: announce it LOUDLY as a checkpoint (id, rung, nodes, tt_hits, wall time). This closes the last WIN entry.
4. ONLY IF step 3 fails (memory or 20M-exhaustion): the fallback is the frontier-fragment leaf-solve architecture already scaffolded in HEAD — bounded FIXED-HORIZON probes (thousands of nodes each, not 512), memoized once per position, with probe nodes accounted HONESTLY against the rung budget (20M gives room for ~2,000 probes x 5k nodes + interior). Tune it on the hard child from step 2, not on full 0l. This is the only sanctioned new-machinery lane; do not open others.
5. CONSOLIDATION (after 0l is banked, or after step 4 concludes either way): per the owner's one-system directive, every cfg(test)/env-gated experiment in HEAD that is not part of the winning path gets DELETED (macro-DFS, DAG PN, sequential-small-choice, and the frontier machinery too if step 3 succeeded without it). Keep the engine minimal: WidthOptions narrow/vcf_pair_complete only. Announce a checkpoint after the cleanup.
6. FINAL GATE: full unit suite green; 12-entry <=100k matrix green; lz60mfb at its banked rung; 0l at its banked rung; the 5 expect=NO entries produce zero WIN; narrow/default byte-identity check. Finish with ONE clean full-gate matrix over all 19 corpus entries at DEFAULT settings and report per-entry rung + node counts in .codex-round7/round7-final.md.

Re-verify all previously closed entries after every engine change (a re-run of the 12-entry matrix + lz rung).

OPERATIONAL RULES (binding, unchanged):
1. You CANNOT git-commit (sandbox blocks worktree git metadata). Announce checkpoint boundaries clearly in progress messages; the orchestrator commits. Never treat commit failure as a blocker.
2. ALL cargo target dirs stay on E: inside this worktree (.target-codex/ exists — reuse it). NEVER build under C: temp. Do not delete .target-codex/ mid-round.
3. NO workspace-wide formatting. Use `rustfmt --edition 2021 <file>` on ONLY files you edit.
4. HOST IS SHARED: a live GPU training run and a live Lean/Mathlib Codex campaign share this 32 GB box. CARGO_BUILD_JOBS=4 max; one solve at a time; obey the step-3 memory-watch rules.
5. Work only in .claude/worktrees/tss-vcf-width on branch claude/tss-vcf-width.
6. Keep a running .codex-round7/round7-progress.md updated after EVERY completed step (round 6 went 100 minutes without a note — do not repeat that).
