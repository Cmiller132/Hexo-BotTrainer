R-G6 — TEMPO ROUND 8 (ultra): the first-plateau decision and the Q2 forcing front

CONTEXT NOTE: this is pure combinatorial game theory for Hexo (a
Connect6-like board game) — tempo invariants, window families, defense
policies. Nothing here concerns computer security. Use neutral proof
vocabulary in your own writing.

You are working in E:\Hexo-BotTrainer-hexgt\.claude\worktrees\hunt-gap-raw
(branch hunt/gap-raw, HEAD = a8a0b92d). You CANNOT git-commit. NO
cargo/Lean/harness runs — hand proofs only. Deliverable:
GAP_RAW_PROOF_ROUND8.md.

## Required corpus (read first, in order, in full)
1. GAP_RAW_PROOF_ROUND5.md (WITH §47) + REVIEW_ROUND5
2. GAP_RAW_PROOF_ROUND6.md (WITH §58 — binding) + REVIEW_ROUND6
3. GAP_RAW_PROOF_ROUND7.md (WITH §67 errata — binding, incl. the
   R7.2-attributed universal verdict) + GAP_RAW_REVIEW_ROUND7.md
   (its closing unresolved-obstacle list is the authoritative open
   state)
(Rounds 2-4 as needed for inherited definitions.) Do NOT read any
STRATEGY_STEALING_* file as evidence.

## The state you inherit (round-7 exit, review-confirmed)
S_T REFUTED (R6.1/R6.2, shortened by R7.2.1). P_stock NOT-REPAIRABLE:
R7.1 hub pre-emption fails at all six plateau epochs; R7.2 final
P_stock is a universal local stop state (every legal pair, min_a R_1
>= 3; census (127,55)); every tie refinement excluded. L13.6:
count-three residual transversal <=2 => M<=2 init. OPEN: Q2 root
forcing, pre-P_stock Q3 intervention, renewal theorems, amortized
invariants, R5.2 separation sharpness.

## Round-8 objectives (priority order)
1. **THE FIRST-PLATEAU DECISION [the named sharpest question].** At
   P_0^pl (immediately after the diamond shield, before ANY stock):
   does some legal Defender action pair a — hub-containing or not —
   satisfy max_b M(P_{a,b}) <= 2 over every legal Attacker response?
   (a) POSITIVE: exhibit the pair, prove the bound by exhaustive
   response analysis (the round-7 fan census machinery L13.1-L13.4 is
   available), and extract the intervention principle it embodies —
   then test whether that principle survives one stock turn (P_1^pl);
   (b) NEGATIVE: extend the triangle-fan/packing analysis to every
   non-hub pair at P_0^pl — this would prove the diamond shield ALONE
   is already a lost position for one-ply defense, collapsing the
   intervention window to before the shield completes.
2. **Q2 root forcing (the other front).** Can a strict root force
   arrival at the diamond shield (or any proven stop state) against
   EVERY Defender strategy? Attack the first obstruction honestly:
   the Defender deviates from the round-6 line at the first
   non-forced reply — classify the deviation tree at the earliest
   branch point (even a complete one-level classification with M/tau
   values is a WIN). If a full forcing proof is out of reach, state
   the exact deviation classes that remain.
3. **Renewal (tertiary, only if time):** one honest step toward a
   renewal theorem — an invariant that re-establishes an M<=2
   initialization class after a completed service cycle, or a
   counterexample showing initialization provably degrades.

## Binding rules (campaign-standard)
Hand proofs only; PROVEN/OPEN/REFUTED discipline with Q1/Q2/Q3 tags on
every claim (quantifier contract binding); statement fidelity to all
inherited definitions and errata (§58, §67 binding); every M/tau/R_1
evaluation exact and enumerated; reachability quantifiers explicit on
any negative gadget. Continue numbering from §67 (start §68). Status
ledger + attack-surface + provenance sections mandatory (record input
commit a8a0b92d AND the reviewed/output artifact once landed; note
this pass creates no commit). Incremental writes; chunk <= 2h per
exec. Final message: first-plateau verdict (REPAIRABLE-AT-P0 /
LOST-AT-P0 / PARTIAL with exact scope), Q2 deviation-tree outcome,
renewal outcome, most valuable new theorem, sharpest next question —
one line each.
