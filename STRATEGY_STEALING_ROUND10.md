# Strategy stealing in engine Hexo, round 10: asynchronous cliff crossing and second-cycle recurrence

**Worktree:** `hunt/gap-raw` at requested input HEAD `88bca52d`  
**Date:** 2026-07-18  
**Cliff verdict:** **PROVEN-POSITIVE ON A DELIMITED ASYNCHRONOUS BRIDGE
CLASS, WITH A PROVEN EXACT OUTER-SLEEVE OBSTRUCTION TO ARBITRARY-S
COVERAGE.**  
**Recurrence verdict:** **PROVEN FOR ONE SECOND CYCLE ON A NONEMPTY
PROTECTED-SPARE-LINE CLASS; INDEFINITE AND ARBITRARY-S RECURRENCE REMAIN
OPEN.**  
**Arbitrary-S boundary:** **PROVEN EXACTLY FOR THE TWO LITERAL INHERITED-`T`
S70 PAIR MODES AT THEIR FIXED CHECKPOINTS.**  
**Global target:** `NL_F` remains **OPEN**.

Round 9 ended at the right cliff but only tested synchronous conversions.  A
bounded asynchronous schedule can do something genuinely different.  It can
pause the shadow at its pending F `SecondStone` query, let the real board
complete that F turn, traverse one whole real S turn, and take the first
placement of the following real F turn.  Both boards are then live again at F
`SecondStone`.  The offset is bounded and the final query uses the inherited
section-53 terminal closure at a genuinely common phase.  The schedule crosses
the S58/S49 cliff exactly when the intervening real-S pair leaves a physical
real-F window with at most two holes.  This is a carrier success and a real-F
outcome on that history; it is not a defeat of `sigma`.

The same theorem exposes its own sharp boundary.  On the S70 bridge, the five
real-F stones occupy five cells of the six-cell seed line.  Before completeness
is claimed, the six possible missing-cell indices are enumerated.  In every
case at most two empty cells hit the hole sets of *all* real F-unblocked
one- and two-hole windows, even if an outer carrier reassigns its sleeve after
seeing the position.  Real S can therefore block every immediate outer sleeve
in one turn.  The ejected object is the bounded asynchronous/outer carrier,
not `sigma` and not every longer-lag carrier.

On the slow side, a separate axis-isolated spare line turns an S70
disposition-3 entrance into one complete further reserve-handler-generated
`R_1` cycle.  A legal nonterminal F pair
puts three real-F stones on that spare line; two inherited-`T` certificate
events then return to common F `FirstStone` with `RES_1`, `tau_E=0`, a fresh
next certificate, and the three-stone line seed intact.  Explicit finite
exclusion bounds, `122<217` and `168<217`, prove the two S choices.  A concrete
diagnostic trace proves nonemptiness.  Terminal prescriptions, wrong-role
occupation of the old certificate, and physical `Shat` completions are
classified at their exact events.

Finally, the word "narrow" in the review-confirmed S70 bypass is replaced by
two finite ordered-pair membership tests.  One covers the empty-debt guarded
pair (`filler,T(u)`); the other covers singleton-debt ingress
(`T(e),T(u)`).  The latter has an S69-style necessary-and-sufficient reserve
formula.  These tests classify every locally legal ordered pair at the two
fixed S70 checkpoints.  They do not handle a pair which fails the literal
inherited-`T` vehicle.

All results are hand proofs.  No Cargo command, Lean command, harness,
executable search, solver, or proof-search program was run.  No `GAP_RAW_*`
file was opened or used as evidence.  No commit is created by this pass.

## 85. Statement boundary and binding inherited state

### 85.1 Roles, target, and status language

The real game has opener `F` and second player `S`.  The shadow game has
opener `Shat` and second player `Fhat`; a fixed pure alleged-winning shadow
strategy is denoted `sigma`.  The inherited strategy-stealing target `NL_F`
remains open.

The proof labels are literal:

- **[PROVEN]** means a hand proof from the binding corpus and production
  rules;
- **[PARTIAL]** is a disposition for a proved strict subclass, never a proof
  status by itself;
- **[OPEN]** is an unresolved obligation; and
- definitions are stipulative.

Every negative event below defeats only the object named in its theorem.
In particular, an `Fhat` win with a failed real transfer defeats a carrier;
it does not defeat `sigma`.  Conversely, a physical `Shat` win on a genuine
candidate-own counterplay defeats alleged-winning `sigma` and is never
continued.

### 85.2 Production boundary [PROVEN]

The inherited production facts used here are:

1. the only opening is at the origin, followed on every nonterminal history
   by `F ; S,S ; F,F ; S,S ; ...`;
2. a normal append is legal exactly when its cell is physically empty and in
   the color-blind radius-eight legal store;
3. a first placement is physically installed before the second is validated;
4. each append is tested immediately against the eighteen incident Q/R/QR
   length-six windows, and a win suppresses every later placement on that
   board; and
5. forward histories are append-only.  A label, portfolio, debt queue, phase
   offset, or homomorphism never erases, moves, or recolors a stone.

Section 53's terminal closure installs the two already-associated appends of
one final paired event before closing the coupled trace.  It supplies no
third placement and no continuation of a terminal engine.  Round 10 invokes
that convention only at a genuinely common phase: after the asynchronous
schedule restores F `SecondStone`, or at the recurrence handler's common F
query when a reached prescription is terminal.

### 85.3 Binding Round-9 state

Sections 44, 53, 63, 73, and 84 and their reviews are binding.  In
particular:

- S70's three-way first-cycle controller is **PROVEN** only on its constructed
  selected history;
- S70.1 is an optional protected positive-horn closure, not an automatic
  consequence of every disposition-3 entrance;
- S70.2 forces each `FAST_8^{S15}` candidate onto S58 one microstep before
  S49, but defeats only the one-for-one carrier;
- S69 is the exact scalar `RES_1` test;
- the same-history reserve, both ordinary real appends, section-53 closure,
  and terminal-moment S63 all fail to convert S49 at their named scopes;
- S49's sixth-stone count barrier, S61's discipline, and the physical stop
  grammar remain mandatory; and
- section 84.3's seven open obstacles are the authoritative caveat ledger.

### 85.4 Q-scopes and completeness discipline

Three scopes are kept separate.

1. `Q_ASYNC^4` is the bounded four-real-append pause schedule of section 86.
   Its bridge S pair must be legal and nonterminal.  It is not a common-phase
   one-for-one carrier during the pause.
2. `Q70^2` is one protected-spare-line cycle after an S70 disposition-3
   entrance.  It selects its S pair and assumes the two reached `sigma`
   prescriptions and the fixed old-certificate append are nonterminal.  The
   later certificate is made nonwinning by the finite selection.
3. `Q_TPAIR` is the finite ledger of every locally legal *ordered* S pair at
   either of two fixed S70 checkpoints, under the literal inherited-`T`
   schedule only.

Before every completeness assertion, the finite index set is stated.  A
ledger can be complete for its grammar while the global branch system remains
open.

## 86. Crossing the S58/S49 cliff

### 86.1 The bounded phase-offset carrier

Start immediately after the nonterminal paired event `Fhat@z_3/F@k_3` on
the line-seeded fast approach.  Both boards are live at F `SecondStone`; the
unchanged shadow prefix has one next prescription `z_4`.  Do **not** query it
yet.

**Definition 86.1 (`ASYNC-CYCLE_4`).**  Freeze the physical shadow board and
advance only the real board according to the following finite schedule.

| index | real append just made | real next phase | frozen shadow phase | unmatched real appends |
|---:|---|---|---|---:|
| 0 | none | F `SecondStone` | `Fhat SecondStone` | 0 |
| 1 | `F@k_4` | S `FirstStone` | `Fhat SecondStone` | 1 |
| 2 | `S@y_1` | S `SecondStone` | `Fhat SecondStone` | 2 |
| 3 | `S@y_2` | F `FirstStone` | `Fhat SecondStone` | 3 |
| 4 | `F@r_1` | F `SecondStone` if nonterminal; otherwise physical F stop | `Fhat SecondStone` | 4 |

The offset discipline is the displayed index set `{0,1,2,3,4}` together
with the two physical phases.  It is exhaustive because each real
nonterminal phase successor is forced while the shadow is unchanged.  On the
live branch, indices 0 and 4 are the only common-role/common-microphase
states, so index 4 is the first possible live resynchronization; a terminal
`r_1` instead closes at its physical append.  The bridge contract requires
`k_4,y_1,y_2,r_1` to be legal and the bridge S pair to be nonterminal.  A
terminal real-S bridge event is an outer P5R/common-only outcome, not a
successful member of this class.

At the real F `FirstStone` state after `Y=(y_1,y_2)`, define the complete
physical catch family

`C_2(R_Y)={W: W is F-unblocked and 1<=delta_R^F(W)<=2}`.     (86.1)

This family is finite: each member contains at least four of finitely many
real-F stones, and each physical stone belongs to eighteen windows.

### 86.2 Exact asynchronous catch

**Theorem S71 (four-offset asynchronous catch) [PROVEN at
`Q_ASYNC^4` terminal scope].**  Suppose the frozen shadow's actual `z_4` is
legal and terminal.  `ASYNC-CYCLE_4` reaches either a sound earlier real-F
stop or a common-phase section-53 co-terminal closure if and only if

`C_2(R_Y) is nonempty`, equivalently `mu_R(R_Y)<=2`.         (86.2)

If (86.2) fails, live phase resynchronization is still legal, but the exact
ejection event is the terminal `Fhat@z_4`: the paired real append is
nonterminal.

*Proof.*  First suppose a surviving real window `W` has deficit two.  Choose
one of its two physical holes for `r_1`.  It is legal because four real-F
stones already support it within line distance at most two.  If `r_1` wins
in a crossing window, that is a sound earlier real-F stop.  Otherwise `W`
has one hole and index 4 has restored the common F `SecondStone` phase.

If `W` instead has deficit one, preserve its unique hole and choose `r_1` as
a legal nonwinning filler.  At this bridge real F has exactly five stones and
real S has six, so the real occupancy before `r_1` has size eleven.  Every
immediate F-winning window must use all five, so its winning-cell set has size
at most two (the S65.1 five-stone census).  A radius-eight ball around any
physical support cell has 217 cells; the eleven occupied cells, those at most
two winning cells, and `W`'s one preserved hole exclude at most fourteen
cells.  A fresh supported nonwinning filler exists.

At index 4 the shadow board is byte-for-byte the physical prefix that was
frozen.  Querying the fixed pure `sigma` now returns the same legal terminal
`z_4`.  Pair it with `W`'s unique real hole at common F `SecondStone`.
Both physical appends win inside section 53's ordinary atomic final event.

Conversely, if every real F-unblocked window has pre-`r_1` deficit at least
three, the two remaining real appends `r_1,r_2` can lower any one window's
deficit by at most two.  Neither an earlier real win nor a real win paired
with `z_4` is possible.  The shadow terminal therefore ejects this carrier.
QED.

S71 genuinely lies outside S58: it is neither query-first nor one-event-per-
microstep nor one-for-one during the pause.  It crosses by adding legal real
history and returning to a common phase, never by treating five stones as
six.  Its positive conclusion is carrier success and a physical real-F win;
it is not a counterstrategy win over `sigma`.

### 86.3 Nonempty crossing witness

**Lemma S71.1 (S42-based asynchronous crossing) [PROVEN].**  The positive
class of S71 is physically nonempty.

Use S42's genuine terminal cylinder through the paired first event

`shadow Fhat@(5,0) / real F@(3,0)`.

The real F stones are then `(0,0),(1,0),(2,0),(3,0)`, while the unchanged
shadow is at F `SecondStone` with actual next prescription `(6,0)`.  Freeze
it and append on the real board

```text
F@(4,0);
S@(6,2), S@(7,2);
F@(8,2).
```

The two S cells are fresh, adjacent, and nonterminal; the six real-S stones
are not collinear.  The real preparation `(8,2)` is supported by `(7,2)`, is
off the q-axis reserve, and is nonwinning.  Both boards are now at F
`SecondStone`.  Query the unchanged shadow and pair

`shadow Fhat@(6,0) / real F@(5,0)`.

The shadow fills `(1,0),...,(6,0)` and the real board fills
`(0,0),...,(5,0)`.  Both wins are physical.  The displayed on-path shadow
prescriptions extend to a total legal pure strategy, but that strategy is not
claimed alleged-winning. QED.

The same schedule crosses every S70.2 line-seeded fast cliff on a selected
legal nonterminal bridge pair which leaves `W_0`, or another member of
`C_2`, unblocked.

### 86.4 Outer correspondence contract

The outer candidate deliberately has no stone map.

**Definition 86.2 (`OUTER-SLEEVE_2`).**  Immediately after the unmatched
real `k_4`, let `Omega` be any real support region, disjoint from or partially
overlapping any spatial image of the shadow fast line.  A sleeve portfolio is
a finite family `P` of actual real F-unblocked engine windows in `Omega`, each
with physical hole set

`H_W=W\X_F`, with `1<=|H_W|<=2`.                            (86.3)

Its exact stuttering homomorphism contract is:

- the pending shadow terminal window has rank `1 -> 0`;
- a real rank-two sleeve may use the unmatched preparation
  `2 -> 1`;
- a real rank-one sleeve may stutter `1 -> 1` while the preparation is a
  filler;
- the final common-phase paired event must make `1 -> 0` physically;
- a real-S stone in `W` deletes that sleeve; and
- no real sleeve stone, bridge S stone, or preparation is asserted to be the
  image of a shadow stone.

Thus the correspondence preserves a window-deficit transition, not cells,
support, or inverse legality.  Reassigning among the full current family is
allowed after every observed bridge placement.

**Lemma S72 (outer sleeve survival criterion) [PROVEN].**  For an actual
legal nonterminal bridge pair `Y={y_1,y_2}`, a fixed sleeve portfolio crosses
within the next real F turn exactly when

`there exists W in P with Y intersect H_W=empty`.           (86.4)

If `P` is the full family of all one- and two-hole real windows, (86.4) is
equivalent to (86.2).  Consequently the exact ejection condition is that
`Y` is a transversal of the physical sleeve-hole family.

*Proof.*  Real S changes no F deficit.  It blocks exactly the F-unblocked
windows it meets, and it can meet one only at a physical hole.  Every
surviving one- or two-hole window is filled by the S71 preparation/final
pair.  Conversely, any real win obtained with those at most two F appends
comes from a window having at most two holes before them, and that window had
to survive the S pair. QED.

This is the requested homomorphic contract.  It is not a causal explanation
for a general outer strategy; it states exactly what this natural immediate
outer sleeve must preserve.

### 86.5 Enumeration before the outer completeness theorem

On the S70 line-seeded bridge, after unmatched `k_4`, real F has exactly five
stones.  They occupy five cells of one six-cell window `W_0`.  Normalize its
axis indices to `[0,5]` and let `g` be its unique empty index.  Any real
F-unblocked window with deficit at most two contains at least four of these
five F stones.  A different engine line meets the axis of `W_0` in at most
one cell, so every such window is an interval on this same axis.

The finite configuration index is therefore exactly `g in {0,1,2,3,4,5}`.
For each index, enumerate every length-six interval containing at least four
of `[0,5]\{g}`.  The following table gives its possible starts and a set of
at most two physically empty cells meeting every hole set.

| missing `g` | possible interval starts | two-cell-or-smaller transversal |
|---:|---|---|
| 0 | `-1,0,1,2` | `{0,6}` |
| 1 | `-1,0,1,2` | `{1,6}` |
| 2 | `-1,0,1` | `{2}` |
| 3 | `-1,0,1` | `{3}` |
| 4 | `-2,-1,0,1` | `{-1,4}` |
| 5 | `-2,-1,0,1` | `{-1,5}` |

This is exhaustive: an interval starting farther left or right contains at
most three of the five marked indices.  Direct substitution verifies the
listed hitting sets.  If a listed cell is already S-owned, every window it
would hit is already S-blocked and is deleted from the actual family; remove
that coordinate from the transversal.  No listed coordinate is F-owned.

### 86.6 The five-stone outer obstruction

**Theorem S73 (two-move outer-sleeve barrier) [PROVEN at the S70 fast-bridge
scope].**  At the post-`k_4` S70 bridge, the hole hypergraph of *all* physical
real F-unblocked deficit-at-most-two windows has transversal number at most
two.  Hence real S has a legal turn which either wins physically for S or
blocks every sleeve in every disjoint or partially overlapping
`OUTER-SLEEVE_2` portfolio.  On the nonterminal blocking branch the reached
real F `FirstStone` state has `mu_R>=3`, so S71 ejects at the pending
`Fhat@z_4` event.

*Proof.*  The six-case enumeration supplies the transversal.  Each retained
transversal cell is a physical empty hole of a window containing at least
four real-F stones, hence is legal through line support.  Before the bridge
real S has four stones, so its first selected append cannot win.  If the
second wins, the proposed F carrier has failed in the actual real game.  If
it is nonterminal, every one- or two-hole F window is S-blocked.  When the
transversal has one cell, play it first and choose a supported nonwinning
padding cell: with five S stones there are at most two immediate S-winning
cells, while a radius-eight support ball has 217 cells.  If no retained
transversal cell remains because old S stones already block the full sleeve
family, choose any fresh legal first padding cell (S then has only five stones
and cannot yet win) and a fresh legal second padding cell.  A win on the
second is a physical real-S stop; a nonterminal second append leaves the
already empty sleeve family empty.  Thus every zero-, one-, or two-cell ledger
case supplies a complete legal S turn.  The full near-window family is empty
on every nonterminal branch, proving `mu_R>=3` and invoking the negative
direction of S71. QED.

A genuinely disjoint sleeve cannot escape this proof: without four of the
five physical real-F stones it does not even enter the rank-one/rank-two
portfolio.  Partial overlap and post-hoc window reassignment are already in
the enumerated full family.

### 86.7 Cliff target disposition

**Target 1: PARTIAL, with a positive crossing and a new exact obstruction.**
S71 crosses the forced S58/S49 cliff on a nonempty bounded-offset class and
finishes with a physical real-F win.  S73 proves why this carrier cannot be
promoted to arbitrary-S coverage: one bridge turn can delete every immediate
outer sleeve.  Longer offsets, more than one inserted real cadence cycle,
outer contracts encoding a deeper real strategy rather than an immediate
two-move sleeve, and the global post-cliff outcome remain **OPEN**.

Neither half changes the status of `sigma`.  On S71's positive branch both
F roles win.  On S73's negative branch the asynchronous/outer carrier is
ejected; the pending shadow prescription and `sigma` remain valid.

## 87. S70 recurrence through a second cycle

### 87.1 The protected-spare-line class

Fix a non-cliff, non-`Shat`-terminal S70 disposition-3 entrance `p`.  Its
physical data are:

- common-live F `FirstStone` cadence;
- real/shadow role counts `(F,S)=(5,6)` and
  `(Fhat,Shat)=(6,7)`;
- singleton debt `E_S={e}` with `c=T(e)` fresh and shadow-legal;
- `RES_1` and `tau_E=0`; and
- Round 9's actual fixed-order least entrance real F-unblocked deficit-one
  reserve `W_in`.

The last bullet is the protected variant used below; it is not silently
inferred as an automatic S70.1 closure for every bare entrance.

**Definition 87.1 (`Q70^2`, protected-spare-line quiet class).**  In
addition, require a real F-unblocked engine window `W_1` containing exactly
one real-F anchor `x`, with its other five cells empty, and require its full
supporting engine axis to contain no other real-F stone and at least two of
its holes to lie outside `W_in`.  In the fixed window/cell orders choose the
first qualifying `W_1` and first two such holes `p_1,p_2`.  Before play,
specialize S59's totalized reserve
handler at this finite prefix as follows.  Before query `z_{4+i}`, let `V_i`
be the handler's current fixed-order least deficit-one real reserve; it exists
because `W_in` survives every earlier nonterminal filler.  Pair a terminal
prescription with `V_i`'s unique hole under section 53; on the nonterminal
continuing branch use `p_i` as the handler's fixed legal filler.  This is a
prefix-causal continue/stop rule, not a retrospective relabeling.  Require the
actual next sequential
prescriptions `z_5,z_6` to be nonterminal and different from `c`, and require
the fixed old-certificate append `Shat@c` to be nonterminal.  Nonterminality
of the later `Shat@T(y_5)` is proved by the selection below, not assumed before
`y_5` is bound.

The chronological event index, stated before the recurrence claim, is

`J_2={F_1,F_2,S_1,S_2}`.                                   (87.1)

At `F_i` the real append is `F@p_i` and the shadow append is
`Fhat@z_{4+i}`.  At `S_1,S_2` the associated shadow appends are
`Shat@c, Shat@T(y_5)`.  This four-event list is exhaustive for one engine F
pair followed by one engine S pair.

### 87.2 The spare seed pair

**Lemma S74 (axis-isolated one-anchor seed pair) [PROVEN].**  At `p`, the
real pair `F@p_1,F@p_2` is legal and necessarily nonterminal.  If the paired shadow
prescriptions satisfy Definition 87.1, both boards reach common S
`FirstStone`; `W_1` then contains exactly three real-F stones and three fresh
holes.  The old debt and `tau_E=0` are unchanged, while `W_in` keeps
`mu_R=1`; indeed the entire deficit-one family is unchanged, so `RES_1` still
holds and the handler keeps selecting `W_in`.

*Proof.*  Both holes are within line distance at most five of anchor `x`, so
they are sequentially legal.  On `W_1`'s full axis, the first append sees only
`x,p_1`; every other axis through `p_1` misses `x` and can contain at most the
four old F stones off the supporting axis plus `p_1`, only five stones total.
Thus `p_1` cannot win.

After `p_1`, `W_1`'s full axis contains only `x,p_1,p_2` among the relevant
F stones.  Every other axis through `p_2` misses both `x` and `p_1`, so it can
again contain at most the four old off-axis F stones plus `p_2`, only five.
Thus `p_2` is nonwinning.

For the stronger reserve claim, all five old F stones lie on `W_in`'s axis.
The distinct `W_1` axis has only `x,p_1,p_2` after the pair.  Any axis of the
third direction meets each of those two axes at most once; an axis parallel
to either meets at most the other one once.  Thus every remaining axis has at
most two real-F stones.  The same bounds hold after only `p_1`.  Therefore no
new deficit-one window is created, while every old one is unchanged because
the fillers are off the old-F axis.  No S stone or debt changes. QED.

On the live branch, let `W_+` be S59's fixed-order least real F-unblocked
window attaining the post-F minimum.  S74 proves the exact identity
`W_+=W_in`; it is the designated post-F reserve which the returning S pair
must avoid.  The same unchanged-family fact makes `V_1=V_2=W_in` in
Definition 87.1.

### 87.3 Finite selection architecture for the returning S pair

After Lemma S74, the physical occupancies have sizes

`|O_R|=13` and `|O_H|=15`.                                 (87.2)

Real S still owns six stones.  Let `B_4` be the set of non-S cells lying in
an engine window which already contains at least four real-S stones.  The
six S stones have `18*6=108` window incidences.  There are at most 27 such
windows and each has at most two non-S cells, so

`|B_4|<=54`.                                               (87.3)

The old certificate `c` and the shadow prefix are already fixed.  Evaluate
the stipulated nonterminal append `Shat@c` prospectively, without installing
it before its real partner.  Shat would then own eight stones.  Let `G_c` be
the set of fresh cells whose next Shat append would complete a window.  Every
such window contains at least five of those eight stones.  The `18*8` stone-
window incidences therefore give at most `floor(144/5)=28` such windows, hence
at most 28 such winning cells (each live winning window has one empty hole),
and

`|G_c|<=28`.                                               (87.3a)

This fixed-prefix evaluation is causal: `c` is determined before `y_5`, and
the real choice does not alter the shadow board.  No shadow stone is appended
out of engine order.

Choose `y_5` from the 217-cell ball `B_8(e)` outside

`O_R union T^{-1}(O_H) union W_+ union W_1 union B_4`  
`    union T^{-1}(G_c)`.

The union bound is

`13+15+6+6+54+28=122<217`.                                (87.4)

After `S@y_5/Shat@c`, the occupancy sizes are 14 and 16 and real S owns
seven stones.  Let `B_3` be the set of non-S cells lying in an engine window
which already contains at least three of those seven stones.  There are at
most `(18*7)/3=42` such windows and at most three non-S cells per window, so

`|B_3|<=126`.                                              (87.5)

Choose `y_6` from `B_8(y_5)` outside

`O_R union T^{-1}(O_H) union W_+ union W_1 union B_3`.

Now the bound is

`14+16+6+6+126=168<217`.                                  (87.6)

These are union bounds, not assertions that the excluded sets are disjoint.
They state the complete finite selection architecture used in the proof.

### 87.4 Second-cycle recurrence

**Theorem S75 (one full S70 second cycle) [PROVEN on the nonempty
`Q70^2` class].**  Every `Q70^2` entrance admits the Lemma-S74 F pair and a
causal inherited-`T` S pair `y_5,y_6` selected by (87.4)--(87.6).  The old
certificate is nonwinning by the class premise and the current certificate
is nonwinning by (87.3a)--(87.4).  The exit is a common-live F `FirstStone`
state with

1. scalar `RES_1`;
2. singleton debt `E_S={y_6}` and `tau_E=0`;
3. a fresh shadow-legal next image `T(y_6)`; and
4. the F-unblocked spare seed `W_1` containing exactly three real-F stones
   and three fresh legal holes.

Thus the axis-isolated line present at the first-cycle horn supplies a legal
nonterminal handler-generated F pair, and the certificate cadence returns
once to the full current entrance predicate.  The exit retains a
three-F/three-hole spare line, but this theorem does not declare a third-cycle
pair on that line nonterminal.

*Proof.*  Lemma S74 gives the common S `FirstStone` state.  Choose `y_5` by
(87.4).  It is real-empty and legal through the occupied debt cell `e`.
Its image is shadow-empty.  Because `y_5` lies in no `B_4` cell, every
F-unblocked window through it contains at most four S stones after the
append, so its post-placement S deficit is at least two: the first placement
is first-safe.

The old image `c=T(e)` remained fresh because `z_5,z_6` avoided it, and it
remained legal because its physical support persists.  Append `Shat@c`.  By
the class premise it is nonwinning and physically certifies `e`.  Since
`d(y_5,e)<=8`, `T(y_5)` is now shadow-legal; it is fresh because `y_5` was
chosen outside `T^{-1}(O_H)`, while `y_5 != e` and injectivity give
`T(y_5) != c`.  It is nonwinning because `y_5` was also chosen outside
`T^{-1}(G_c)`.

Choose `y_6` by (87.6).  It is real-legal through `y_5`, its image is fresh,
and it avoids both protected windows.  Since it lies outside `B_3`, every
window through it has at most three real-S stones after the append.  It is
therefore nonterminal, and no E-live window through the final debt can have
deficit at most two.  Hence the urgent family is empty and `tau_E=0`.

Append the nonwinning physical certificate `Shat@T(y_5)`.  Both boards pass
to F `FirstStone`.  The next image `T(y_6)` is fresh (the inverse-occupancy
exclusion plus injectivity) and is legal through `T(y_5)`.  Both real-S cells
avoid `W_+`, so S69 preserves `RES_1`.  They also avoid `W_1`, leaving its
three F stones and three empty holes unchanged.  Definition 87.1 made the F
pair reserve-handler-generated; the displayed safety, certificate, service,
reserve-avoidance, nonterminality, and common-exit facts verify the remaining
S59 cycle clauses. QED.

The inherited transient two-debt state is not skipped.  Between
`S@y_5` and `Shat@c`, every old E-live window still satisfies

`delta'-m'=(delta-m)+1-1_{y_5 in W}>0`.

The same first-safety argument covers the second microstep before
`Shat@T(y_5)` is installed.

### 87.5 Nonempty diagnostic trace

The class is not merely conditional syntax.  Take `T(q,r)=(q+2,r)` and the
following physical continuation; extend every displayed `Fhat` value by the
least legal off-path rule.

```text
S15:
 real:   F@(0,0); S@(0,1),S@(0,2)
 shadow: Shat@(0,0); Fhat@(1,0),Fhat@(2,0);
         Shat@(2,1),Shat@(2,2)

first line seed:
 real:   F@(1,0),F@(2,0)
 shadow: Fhat@(3,0),Fhat@(4,0)

guarded pair:
 real:   S@(3,3),S@(5,2)
 shadow: Shat@(0,1),Shat@(5,3)

second F pair:
 real:   F@(4,0),F@(5,0)
 shadow: Fhat@(-8,0),Fhat@(-7,0)

protected ingress:
 real:   S@(8,4),S@(10,5)
 shadow: Shat@(7,2),Shat@(10,4)

second-cycle F pair:
 real:   F@(0,-5),F@(0,-4)
 shadow: Fhat@(-6,0),Fhat@(-5,0)

second-cycle S pair:
 real:   S@(13,7),S@(16,8)
 shadow: Shat@(12,5),Shat@(15,7)
```

Use

`W_0={(q,0):0<=q<=5}` and `W_1={(0,r):-5<=r<=0}`.

At the first disposition-3 entrance, `W_0` has unique hole `(3,0)`, the debt
is `(10,5)`, and its fresh legal image is `(12,5)`.  `W_1` is clean except
for anchor `(0,0)`, and its full `q=0` axis contains no other real-F stone.
Here `W_in=W_+=W_0`; it is the unique deficit-one window before and after the
vertical seed pair.  That pair makes `W_1` a three-F/three-hole seed.  The
displayed S pair avoids both windows, both certificates are fresh,
legal, and nonwinning, the final debt is `(16,8)`, and `(18,8)` is its fresh
legal image.  Direct axial differences give support distance at most eight
for every append; no displayed owner has six on an engine axis before the
claimed endpoint.  The final urgent family is empty by the same no-three-old-
stones-through-`y_6` test used in S75.  This proves physical nonemptiness for
a legal diagnostic strategy, not for an alleged-winning one.

### 87.6 Exact obstruction events

The chronological index (87.1), preceded by engine terminal priority, gives
the complete boundary ledger for the totalized candidate.  `Q70^2` itself is
the quiet continuing row; the bullets also name the first events which leave
that selected row.

- At `F_i`, a terminal `z_{4+i}` invokes Definition 87.1's reserved
  unique-hole action on the current `V_i`.  Section 53 installs a physical
  co-terminal F/`Fhat` event.  The recurrence stops soundly rather than re-establishing an
  entrance; neither the handler nor `sigma` is defeated.
- A nonterminal `z_{4+i}=c` gives the old certificate persistent wrong-role
  `Fhat` occupancy.  S53 cannot recognize it as a `Shat` certificate; this
  inherited-`T` recurrence ejects at that exact append.
- The axis-isolated real seed pair itself cannot terminate by S74.
- At `S_1` or `S_2`, a certificate-created `Shat` win is a physical stop and,
  on a genuine alleged-winning history, the direct contradiction.  It is not
  continued.  The selected `Q70^2` construction assumes this away at `S_1`
  and excludes it at `S_2` by (87.3a)--(87.4).
- If all four events are nonterminal and no wrong-role collision occurs,
  S75 proves re-establishment.  Failure of the spare-line, finite-selection,
  reserve-avoidance, or certificate premises is outside `Q70^2`; it is not an
  impossibility theorem.

There is also a sharp literal-reuse boundary.  `W_in` has deficit one, so
playing its unique hole as the first seed append is an immediate sound real-F
win and leaves no nonterminal second seed hole.  A continuing second cycle
therefore needs a separate spare line.  This preserves S70.1's status: the
win is an optional closure on the protected subclass, not an automatic
consequence claimed for every bare S70 entrance.

### 87.7 Recurrence target disposition

**Target 2: PROVEN for one nonempty second-cycle class; PARTIAL globally.**
S75 is the requested second-cycle recurrence theorem with an axis-isolated
line seed, legal reserve-handler-generated first F pair, cadence alignment,
`RES_1`, `tau_E=0`, and a fresh next certificate.  It selects the S pair,
assumes quiet/nonterminal prescriptions, and proves only one repetition.
Arbitrary-S and indefinite recurrence remain **OPEN**.

## 88. Exact inherited-`T` S-pair coverage on the S70 line

### 88.1 Enumeration architecture before completeness

Fix one of the two finite physical S70 checkpoints below.  Its real legal
store is finite because it is a finite union of radius-eight balls.  The
exhaustive engine index is the ordered-pair set

`I(p)={(u,v): u in L_R(p), v in L_R(p+S@u)}`.              (88.1)

It is ordered because the board, phase, legal store, windows, and shadow
certificate state are recomputed after `u`.  An unordered two-element set is
not a complete engine index.

Completeness in this section means completeness over (88.1) for the literal
fixed-`T`, common-phase, one-shadow-append-per-real-append vehicle.  A failed
gate ejects that vehicle; it does not refute another carrier or `sigma`.

### 88.2 Empty-debt guarded mode

Let `p_0` be S70's state after its first line-seed F pair.  The counts are

`(F,S)=(3,2)` and `(Fhat,Shat)=(4,3)`,                    (88.2)

at common S `FirstStone`, with `E_S=empty`.  The three real-F stones lie in
the chosen window `W_0`.  For each legal first coordinate `u`, let `f(u)` be
the fixed first fresh shadow-legal filler distinct from `T(u)`.  The literal
shadow schedule is

`Shat@f(u), Shat@T(u)`.                                    (88.3)

The two shadow appends raise `Shat` only to four and five stones, and the two
real appends raise S only to three and four.  Thus all four are nonterminal
whenever the cells are legal.

For an ordered pair `(u,v)`, define

`D_0(u,v)={W: v in W, W intersect X_F=empty,`  
`           |W intersect (X_S(p_0) union {u,v})|>=4}`.      (88.4)

There are exactly four real-S stones at the endpoint.  Consequently

`tau_E=0 iff D_0(u,v)=empty`.                              (88.5)

**Theorem S76 (exact guarded-pair boundary) [PROVEN at fixed `p_0`].**  An
ordered pair `(u,v)` gives a live literal inherited-`T` guarded prefix which
ends at common F `FirstStone`, carries singleton debt `{v}`, has a fresh legal
next image, preserves the named line `W_0`, and has `tau_E=0` if and only if:

1. `(u,v) in I(p_0)`;
2. after `Shat@f(u)`, the cell `T(u)` is fresh and shadow-legal;
3. after `Shat@T(u)`, the cell `T(v)` is fresh and shadow-legal;
4. `D_0(u,v)=empty`; and
5. `u,v notin W_0`.

*Proof.*  Necessity follows from the two physical appends, the outstanding
debt's certificate, (88.5), and physical preservation of `W_0`.  Conversely,
the count observation makes every listed legal append nonterminal.  The two
boards therefore pass through `SecondStone` to common F `FirstStone`.
Condition 3 supplies the current debt image, condition 4 gives `tau_E=0`, and
condition 5 keeps all three F stones in the F-unblocked seed. QED.

The fixed named line is sufficient but not necessary if causal line
reselection is allowed.  Let

`A_3(p_0)={W in U_R^F(p_0): delta_R^F(W)=3}`               (88.6)

and, for the first coordinate `u`,

`B_u^3={W in A_3(p_0):u notin W}`.                         (88.7)

Then the exact alternative-line survival criterion is

`exists W in A_3(p_0) with W intersect {u,v}=empty`  
`iff B_u^3 is nonempty and v notin intersection(B_u^3)`.   (88.8)

Here `intersection(B_u^3)` means the intersection of its member windows; the
nonempty-family clause is explicit.  Formula (88.8) is the same physical
deletion identity as S69: S adds no F stone and deletes exactly the
F-unblocked real windows it meets.  Since three total real-F stones force
`mu_R>=3`, a surviving member gives exact exit minimum three.

For continuation through the actual next F pair, one further gate is exact.
Prospectively evaluate the fixed pure `sigma` after the already fixed
`f(u),T(u)` shadow prefix.  On a nonterminal, non-S58 continuation its actual
`z_3,z_4` must leave `T(v)` empty and legal.  S70's selected `v` enforces
this by inverse-occupancy exclusion.  A terminal or S58 disposition has its
own earlier stop/ejection and does not pretend to reach the later certificate
checkpoint.

### 88.3 Singleton-debt ingress mode

Let `p` be S70's non-cliff state immediately before its protected ingress S
pair.  It is common-live at S `FirstStone`, satisfies `RES_1`, has
`E_S={e}`, has current image `T(e)` fresh and legal, and has S70's designated
post-F deficit-one reserve `W_*`.  Its counts are

`(F,S)=(5,4)` and `(Fhat,Shat)=(6,5)`.                    (88.9)

The literal associated schedule for `(u,v)` is

`(S@u / Shat@T(e)), (S@v / Shat@T(u))`.                   (88.10)

For a first coordinate `u` satisfying the sequential real/image liveness
gates stated below, let `H_u` be the shadow state after `T(e),T(u)` and put

`a(u)=1 if mu_H(H_u)=1, and a(u)=2 if mu_H(H_u)>=2`.       (88.11)

Using the *pre-pair* real state, define

`A_a(p)={W in U_R^F(p):delta_R^F(W)<=a}`,                  (88.12)

`B_u={W in A_{a(u)}(p):u notin W}`.                        (88.13)

S69 gives the promised exact scalar membership formula:

`RES_1 holds at the exit`  
`iff B_u is nonempty and v notin intersection(B_u)`.       (88.14)

Indeed, the right side says exactly that some adequate old real window avoids
both S cells.  No empty-intersection convention is allowed to hide the first
conjunct.

**Theorem S77 (exact inherited-`T` ingress boundary) [PROVEN at fixed `p`].**
An ordered pair `(u,v)` is admitted by the live first-safe literal
inherited-`T` ingress vehicle and reaches a common-phase scalar checkpoint
with `RES_1`, singleton debt `{v}`, a fresh legal next image, and `tau_E<=1`
under schedule (88.10) if and only if all six gates hold.  Gates 1--3 are
evaluated first; only on their live branch are `H_u`, `a(u)`, and `B_u`
defined.

1. **Real order:** `u` is legal and first-safe (`d_u>=2`), and `v` is legal
   on the reached real `SecondStone` board and nonterminal (`d_v>=1`).
2. **Old certificate:** `T(e)` is fresh, legal, and nonwinning.
3. **Current copy:** after that append, `T(u)` is fresh, legal, and
   nonwinning.
4. **Next certificate:** at the common-live exit, `T(v)` is fresh and
   shadow-legal.
5. **Service:** the endpoint singleton debt `{v}` has `tau_E<=1`.
6. **Scalar reserve:** formula (88.14) holds.

For S70's stronger scalar endpoint replace gate 5 by `tau_E=0`.

**Corollary S77.1 (strict current-`R_1` boundary) [PROVEN at fixed `p`].**
The literal schedule reaches S70's full strict current-`R_1` entrance
predicate if and only if gates 1--6 hold and the seventh gate

7. **Designated reserve:** `u,v notin W_*`

also holds.  Gate 7 is sufficient for gate 6 by S69, but retaining both makes
the scalar/strict distinction explicit.  Gate 7 is not necessary for scalar
survival: another member of `A_{a(u)}(p)` may avoid both cells even when the
designated reserve is hit.

This is a state-level entrance classification in S70's sense.  It does not
retroactively assert that S70's preceding custom line-seed segment was already
a member of the S59 concatenation class `R_1(sigma)`.

*Proof.*  The finite engine index supplies the sequential real legality.
First-safety and the inherited transient formula protect the old debt between
`S@u` and `Shat@T(e)`.  The latter physical stone certifies `e`.  At the
second microstep, real nonterminality and the legal nonwinning
`Shat@T(u)` certify `u`; both boards pass to common F `FirstStone` with only
`v` in debt.  Gates 4 and 5 supply the next physical certificate and service
state.  Gate 6 is necessary and sufficient by S69.  This proves sufficiency
for S77.  Reading the same physical events backwards through the literal
grammar proves necessity.  S77.1 is then exactly S69.1's additional
designated-reserve condition. QED.

If either certificate append wins, that is a physical `Shat` stop, not a live
membership failure.  If a real append wins, the applicable P5R or distinct
common-only transfer is tested at that event.  Wrong-role occupancy,
fresh-illegal images, unsupported images, `tau_E>1`, and scalar reserve break
are exact ejections of this inherited-`T` vehicle; none is automatically an
ejection of another vehicle.

### 88.4 Small finite ledgers for applying S76--S77.1

The reserve part of S77 is especially small on the S70 line.  Normalize the
five real-F cells to

`P={0,1,2,3,4,5}\{h}`

on one axis.  Every deficit-at-most-two real F-unblocked window lies on that
axis.  For threshold `a in {1,2}`, enumerate starts

`J_a={s: |P intersect [s,s+5]|>=6-a,`  
`          [s,s+5] is F-unblocked in p (disjoint from X_S)}`. (88.15)

`J_1` has at most two intervals and `J_2` at most five.  Every possible start
lies in `{-2,-1,0,1,2}`.  Formula (88.14) is therefore a check against at
most five real windows; the strict S77.1 ledger additionally checks the two
literal incidences `u,v notin W_*`.

The service part is also finite before any completeness claim.  Only the
eighteen windows through the final debt `v` can be urgent.  Let

`U_v={W: v in W, W intersect X_F=empty, delta_R^S(W)<=2}`.  (88.16)

Then

- `tau_E=0` exactly when `U_v` is empty; and
- `tau_E<=1` exactly when `U_v` is empty or the physical hole sets
  `W\X_S`, for `W in U_v`, have nonempty common intersection.

Equations (88.14)--(88.16), together with the sequential image gates, are a
finite membership decision for every index in (88.1).  They replace the
adjective "narrow" by an exact local boundary.

### 88.5 Arbitrary-S target disposition

**Target 3: PROVEN at fixed S70-line, one-turn, literal inherited-`T`
scope.**  S76 classifies every empty-debt guarded pair for the named line;
S76 together with (88.8) classifies fixed-line versus alternative-line
survival.  S77 classifies every singleton-debt pair for scalar membership
within its admitted literal vehicle, and
S77.1 adds the exact full-current-`R_1` entrance gate; (88.14) is the S69-style exact
scalar test.  These are coverage boundaries, not coverage theorems: no
response is supplied for a pair outside the admitted sets, and changing
isometries, nonisometric recoding, phase lag, and outer carriers remain
outside `Q_TPAIR`.

## 89. Result ledger and target dispositions

### 89.1 New theorem ledger

| Result | Status | Exact object and scope | Explicit non-conclusion |
|---|---|---|---|
| S71 four-offset asynchronous catch | **PROVEN at `Q_ASYNC^4` terminal scope** | One frozen shadow query, one complete real cadence bridge, exact catch iff a real one-/two-hole window survives | No arbitrary-S coverage; no defeat of `sigma` |
| S71.1 physical witness | **PROVEN** | One complete S42-based asynchronous co-terminal trace | Diagnostic legal strategy, not alleged-winning |
| S72 outer sleeve criterion | **PROVEN** | Window-rank homomorphism; crossing iff an assigned physical sleeve survives the bridge pair | No stone map and no deeper real strategy |
| S73 five-stone sleeve barrier | **PROVEN at S70 fast bridge** | Six-case exhaustive missing-cell ledger; all immediate sleeves have an at-most-two-cell transversal | Ejects the carrier only; longer offsets remain open |
| S74 axis-isolated spare seed | **PROVEN** | A one-anchor window whose full axis has no other F stone supports a forced-nonterminal real F pair | Existence is a `Q70^2` class premise, not universal |
| S75 second-cycle recurrence | **PROVEN on nonempty `Q70^2`** | One complete further reserve-handler-generated `R_1` cycle; common phase, `RES_1`, `tau_E=0`, fresh certificate, three-F seed | No third cycle, arbitrary S, or arbitrary `sigma`-pair admission |
| S76 guarded-pair boundary plus (88.8) | **PROVEN at fixed S70 checkpoint** | Complete ordered-pair test for `f(u),T(u)` mode; named-line and alternative-line criteria kept distinct | No handler for rejected pairs |
| S77/S77.1 ingress-pair boundary | **PROVEN at fixed S70 checkpoint** | Complete ordered-pair test for `T(e),T(u)` mode; six-gate admitted scalar-vehicle and seven-gate full-current-entrance boundaries | No global inherited-`T` recurrence |
| `NL_F` | **OPEN** | D2 and all inherited bridges remain available | No determinacy alternative selected |

There are no **SKETCH** or **CONJECTURE** theorem claims in this round.

### 89.2 Per-target disposition

1. **Cliff crossing -- PARTIAL, PROVEN on both sides.**  Within
   `Q_ASYNC^4` and with pending terminal `z_4`, the asynchronous carrier
   crosses and gives a real-F win exactly on bridge histories which retain a
   one-/two-hole physical sleeve.  The outer six-case theorem proves that real
   S can eject every such immediate sleeve in one turn.
2. **S70 recurrence -- PROVEN for one second cycle on a nonempty class.**
   At the first-cycle horn, `Q70^2` supplies an axis-isolated spare line and
   legal nonterminal handler-generated first pair, then returns to common
   cadence, `RES_1`, `tau_E=0`, and a fresh current certificate while
   retaining a three-F spare.  Third-cycle admission and general recurrence
   are open.
3. **Arbitrary-S boundary -- PROVEN locally.**  S76--S77.1 classify all
   ordered pairs under the two literal fixed-`T` schedules at their fixed S70
   S-turns, distinguishing named-line, alternative-line, scalar, and full
   current-`R_1` entrance admission.  Rejected pairs remain an outer coverage
   obligation.

### 89.3 Carrier-versus-strategy ledger

| Event | Object which succeeds or fails | Status of `sigma` |
|---|---|---|
| S71 co-terminal closure | `ASYNC-CYCLE_4` succeeds; real F wins | `sigma` also wins its shadow history; not defeated |
| S71 with post-bridge `mu_R>=3` | asynchronous carrier ejects at terminal `z_4` | unchanged and still valid |
| S73 transversal pair | every immediate `OUTER-SLEEVE_2` carrier ejects, or real S wins | no inference unless a genuine `Shat` win separately occurs |
| S75 certificate append wins | physical `Shat` counterplay stops | on the genuine candidate-own shadow history, alleged-winning `sigma` is defeated at that event |
| S75 boundary `z_5`/`z_6` terminal | reserve handler closes co-terminally through its current `V_i`; recurrence stops soundly | `sigma` wins its shadow history and is not defeated |
| S75 nonterminal `z_{4+i}=c` | literal inherited-`T` recurrence carrier ejects at persistent wrong-role `Fhat` occupancy | unchanged; no strategy conclusion |
| S76/S77/S77.1 gate failure | the corresponding named-line, scalar, or full-current-entrance literal inherited-`T` prefix ejects | no strategy conclusion |

## 90. Complete caveat ledger

### 90.1 Section-84.3's seven obstacles, updated

The descriptions preserve the latest binding erratum.  No scoped advance is
promoted to global closure.

| # | Binding obstacle | Round-10 disposition |
|---:|---|---|
| 1 | **Full per-pair zero-lag coverage:** arbitrary-S recurrence, changing isometries, nonisometric/partial recodings, and indefinite one-repair service. | **EXACT LOCAL BOUNDARY; OPEN globally.** S76--S77.1 decide named-line/alternative-line and scalar/full-current-entrance admission for every pair in two literal inherited-`T` schedules, and S75 executes one selected second cycle. They do not handle rejected pairs or broader recodings. |
| 2 | **Pre-checkpoint and recurring P3 globally:** S70 completed only the named first cycle. | **ONE SECOND-CYCLE ADVANCE; OPEN globally.** S75 supplies a genuine second common-phase cycle. S71 supplies a terminal asynchronous bridge, not recurring one-for-one P3. Arbitrary later cycles remain open. |
| 3 | **Coverage outside strict `A_FS2`:** missing/blocked/wrong-role/illegal/unsupported, phase-lagged, unreflected-terminal, and high-transversal cases. | **ONE PHASE-LAG CLASS CONSTRUCTED, ONE EXACT EJECTION; OPEN generally.** S71 handles a bounded nonterminal pause when a sleeve survives. S73 proves immediate outer sleeves are blockable. Other phase lags and all other listed exits remain. |
| 4 | **P5R and common-only real-win transfer generally.** | **OPEN, unchanged globally.** The S71 bridge assumes its real-S pair nonterminal. S75 selects first-safe/nonterminal S moves. S76--S77.1 classify terminal gates but do not supply a general physical reflection. S14 and S25 remain binding. |
| 5 | **Universal service and lock:** canonical `F-LOCK`, arbitrary service, and recurring portfolio admission beyond quiet `R_1/F-LOCK^+`. | **NEW ASYNCHRONOUS CATCH AND SECOND-CYCLE SEED; OPEN universally.** S71 derives a terminal catch from any surviving two-hole sleeve. S75 uses `tau_E=0` and a noncanonical spare seed. Canonical lock and arbitrary service remain open. |
| 6 | **Universal shadow-F terminal fidelity and the fast outcome:** S70.2 forced the precursor but did not decide post-cliff play. | **POSITIVE ON A SELECTED BRIDGE, NEGATIVE FOR ARBITRARY IMMEDIATE SLEEVES; OUTCOME OPEN globally.** S71 converts a fast terminal after a quiet bridge. S73 proves the bridge can be blocked. No real strategy forces the positive bridge against every S pair. |
| 7 | **Reverse legality for spatial carriers:** inverse lifts owe current support, collision, S13, and S18. | **OPEN.** S71-S73 use temporal/window contracts with no inverse. S75--S77.1 use only explicitly checked forward inherited-`T` images. No reverse spatial carrier is claimed. |

### 90.2 Load-bearing Round-10 limitations

1. **The asynchronous lead is physical.**  Four unmatched real appends are
   installed on the actual real history.  They are not bookkeeping, virtual
   stones, or section-53 credit.
2. **Resynchronization is phase-exact.**  The shadow is frozen before its
   `SecondStone` query.  The real cadence returns to F `SecondStone` only at
   index 4.  Within S71, the final terminal event, and only that event, uses
   section 53.
3. **S58 is bypassed, not contradicted.**  S71 leaves its query-first,
   common-phase, one-event-per-microstep, one-for-one premises during the
   pause.
4. **The bridge S pair is a real vulnerability.**  The future catch cells
   are exposed across an intervening S turn.  S73 is the exact immediate
   manifestation of that S12-type vulnerability.
5. **The catch criterion is about actual physical windows.**  A portfolio,
   interface label, or abstract outer region with no four real-F stones is
   not a sleeve.
6. **The outer theorem has one-cycle depth.**  It rules out arbitrary-S
   promotion of immediate rank-one/rank-two sleeves at the S70 bridge.  It
   does not contradict S71's selected crossing or rule out a longer lag which
   builds a deeper real forcing strategy.
7. **The six-case table is tied to the S70 five-of-six line.**  It is
   exhaustive there.  No unproved general hypergraph theorem is inferred for
   arbitrary later F geometry.
8. **A bridge S win is not ignored.**  It ends the real engine and lies
   outside S71's positive class.  The carrier cannot continue through it.
9. **The spare line is separate and axis-isolated.**  Reusing the deficit-one
   reserve ends in a sound real-F win on the first hole; it does not give a
   nonterminal seed pair.  A merely clean six-cell window is insufficient,
   because a shifted six-window on the same axis could already contain the
   other old F stones.  S74 therefore requires the full supporting axis to
   contain only its anchor among old F stones.
10. **`Q70^2` is selected.**  It assumes nonterminal `z_5,z_6`, their
    avoidance of the current certificate, and a nonterminal old-certificate
    append.  Section 87.6 classifies the boundary events; S75 does not force
    the quiet prescriptions.
11. **The exclusion bounds prove existence, not necessity.**  A pair outside
    the selected complements might still work.  Conversely, failure of a
    stated class premise is not an obstruction unless section 87.6 names the
    exact physical event.
12. **`tau_E=0` is not zero debt.**  S75 exits with `E_S={y_6}` and owes the
    fresh legal physical image `T(y_6)`.
13. **One recurrence is not induction.**  More S stones enlarge the bad
    window families.  The numerical bounds are recomputed only for the
    displayed second cycle.
14. **Pair order matters.**  S76--S77.1 enumerate ordered pairs.  Swapping two
    cells can change first-safety, legal support, image freshness, and a
    first-placement terminal stop.
15. **Scalar survival and full entrance admission differ.**  Formula (88.14) can preserve
    `RES_1` through an alternative window even if the designated `W_*` is
    hit.  Literal strict S59 preservation additionally requires avoiding
    that designated window.
16. **Interface maps are not synthesis.**  S72's homomorphism and
    S76--S77.1's membership formulas state contracts and boundaries.  They do
    not explain away the remaining outer obligations or prove `NL_F`.
17. **Every old stone persists.**  In particular, the paused shadow stones,
    unmatched real appends, reserve anchors, certificate stones, and outer
    blockers remain in all later support and terminal calculations.
18. **No outcome inflation.**  A successful selected-history transfer and an
    arbitrary-S obstruction to that same carrier do not decide the global
    fast game.

### 90.3 Binding regression matrix

| Regression | Round-10 treatment | Remaining boundary |
|---|---|---|
| S12 preannounced real-F cell | The asynchronous bridge exposes its sleeve through a real S turn; S73 supplies an at-most-two-cell blocking turn/transversal | Longer or adaptive outer schedules still owe causality |
| S13 fixed-isometry FIFO frontier | No FIFO or reverse point schedule is used | Every proposal satisfying S13's premises remains excluded |
| S14 literal terminal lag | Positive bridge and recurrence S pairs are required nonterminal/guarded | General lag still owes P5R and common-only transfer |
| S18 reverse illegality | Outer correspondence is per-window; recurrence proves only forward images | Every inverse spatial carrier remains open and must pass S18 |
| S20/S49 shadow terminal mismatch | S71 supplies an actual common-phase catch after one real cadence cycle | S73 blocks arbitrary-S promotion; synchronous and other terminal histories remain open |
| S25 older-surplus real-S win | No label discharges real debt; certificates are physically appended | All nonselected lag/recode histories retain the terminal-memory duty |
| S30 exact `tau_E=5` | S75 stays at `tau_E=0`; S77 merely tests `tau_E<=1` | High-transversal service and reachability remain open |
| S31 six-blocker fence | No permanent fence is installed | Availability, interruption, S occupation, and P3 compatibility remain open |

## 91. Provenance, work boundary, and resume point

### 91.1 Repository identity

- Workspace:
  `E:\Hexo-BotTrainer-hexgt\.claude\worktrees\hunt-gap-raw`
- Branch supplied and observed: `hunt/gap-raw`.
- Required input HEAD: `88bca52d`, resolving at the start of this pass to
  `88bca52d2a52dbcda5da60db81f00f69ad6cfcd7`.
- Deliverable: `STRATEGY_STEALING_ROUND10.md`.
- Commit made by this pass: **none**.
- **Post-review landed artifact placeholder (binding):**
  `LANDING_COMMIT=<POST-REVIEW-FOLD>; GIT_BLOB=<POST-REVIEW-FOLD>;`
  `SHA256=<POST-REVIEW-FOLD>`.

The landed-hash line is intentionally a placeholder because this pass is
forbidden to commit.  A later review fold must replace all three fields with
the first landed, unmodified artifact identity.

Unrelated untracked workspace entries were left untouched.

### 91.2 Binding corpus read in order and in full

The evidence pass read exactly the requested corpus before developing the
Round-10 proofs:

1. `STRATEGY_STEALING_ROUND5.md`, including section 44, then
   `STRATEGY_STEALING_REVIEW_ROUND5.md`;
2. `STRATEGY_STEALING_ROUND6.md`, including section 53, then
   `STRATEGY_STEALING_REVIEW_ROUND6.md`;
3. `STRATEGY_STEALING_ROUND7.md`, including section 63, then
   `STRATEGY_STEALING_REVIEW_ROUND7.md`;
4. `STRATEGY_STEALING_ROUND8.md`, including section 73 and the rewritten
   section-68 interface boundary, then
   `STRATEGY_STEALING_REVIEW_ROUND8.md`; and
5. `STRATEGY_STEALING_ROUND9.md`, including section 84, then
   `STRATEGY_STEALING_REVIEW_ROUND9.md`.

The latest review or folded erratum controls every conflict.  No
`GAP_RAW_*` file was read, quoted, or used as mathematical evidence.

### 91.3 Hand-proof and mutation boundary

- No Cargo, Rust, Lean, harness, solver, executable search, or proof-search
  command was run.
- Only hand proofs, binding-corpus inspection, and the inherited
  review-confirmed production audit support S71--S77.1.
- No prior campaign artifact, engine source, or unrelated workspace entry was
  modified.
- No git commit, reset, checkout, merge, rebase, or history mutation was
  performed.

### 91.4 Most valuable theorem and sharpest next question

**Most valuable new theorem:** S71 together with S73.  The pair gives the
first actual asynchronous crossing of the forced cliff and the exact
two-move physical obstruction preventing that vehicle from covering every S
pair.  S75 is the strongest recurrence advance: a genuine second cycle, not
another first-entrance restatement.

**Sharpest next question [OPEN].**  Can a bounded carrier survive successive
S-pair sleeve transversals long enough to create a real threat family of
transversal number at least three, while preserving P5R, common-only real-win
transfer, and a live resynchronization phase; or does a general finite-offset
threat-transversal theorem eject every such carrier before the pending fast
terminal prescription is appended?

The secondary recurrence question is whether the protected-spare-line and
quiet-prescription premises of `Q70^2` can be forced or repaired after every
S70 entrance, rather than selected for one further cycle.
